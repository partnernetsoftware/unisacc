# prd 的历史部分（归档）

这里放**已被取代**的规格文字：版本沿革，以及 §3.0 在 14 阶段之前的容量表。
它们的价值是记录当时的口径，不是当前事实——当前数字在 `prd.md` 的 §3.0、§5.5
与 `python3 -m unisa acc`。归档于 2026-09-23。

---

## 一、模型总表（11 阶段时期，≤2026-09-22）

这张表长期自称“由 `unisa/gold.py` 派生”，实际漂过三次：`type` 由 3211 行长到
4275（TYS 13→15），`enc`/`isel`/`abi` 的 op 轴由 46 长到 70，`abi` 去掉 `tls`
又加进 `nrreg` 与 `arg3..arg5`，另有 `regmap`/`tyinfo`/`pfconv` 三个新阶段。
θ 一列是**训练臂**的参数量，而发布路径是构造权重，两者不可比。

全部 11 个模型共用同一个 kernel（[K-1]），只有形状和权重不同。

| 模型 | 工作流位置 | key 空间 | 行数 | emb 维 | 层形状 | 头(类数) | 交互项 | seed | θ |
|---|---|---|---|---|---|---|---|---|---|
| `pp` | 前端 · 预处理 | `dir(9)×defined(2)` | 18 | 6 | `12` | `y(4)` | — | 23 | 274 |
| `lex` | 前端 · 词法 | `c(11)×peek(11)` | 121 | 6 | `12` | `y(10)` | — | 31 | 418 |
| `parse` | 前端 · 语法 | `nt(5)×tok(68)` | 340 | 8 | `16` | `y(35)` | — | 13 | 1451 |
| `type` | 前端 · 类型 | `t1(13)×op(19)×t2(13)` | 3211 | 8 | `32` | `y(14)` | — | 17 | 1622 |
| `scope` | 前端 · 作用域 | `ctx(6)×kind(5)` | 30 | 8 | `16` | `y(7)` | — | 19 | 479 |
| `irsel` | 前端 · IR 选择 | `family(5)×flavor(38)` | 190 | 8 | `16` | `y(34)` | — | 11 | 1194 |
| `enc` | 后端 · 编码形式 | `op(46)×os(3)×arch(2)` | 276 | 8 | `16` | `y(5)` | — | 29 | 893 |
| `reloc` | 后端 · 重定位 | `kind(3)×arch(2)` | 6 | 6 | `8` | `y(3)` | — | 37 | 161 |
| `isel` | 后端 · 指令选择 | `op(46)×arch(2)` | 92 | 12+8 | `24→16` | `form(5),symbol(62)` | — | 3 | 2628 |
| `abi` | 后端 · 调用约定 | `op(43)×os(3)×arch(2)` | 258 | 12+8+8 | `32→20` | `sysno(56),arg0(18),arg1(18),arg2(18),ret(18),tls(4),gate(5)` | bilinear 8d | 5 | 4421 |
| `combo` | 后端 · 合一（替代 isel+abi） | `op(43)×os(3)×arch(2)` | 258 | 16+8+8 | `48→32` | `form(5),symbol(59),sysno(56),arg0(18),arg1(18),arg2(18),ret(18),tls(4),gate(5)` | factor 12d+bilinear 8d | 7 | 10397 |

> 本表由 `unisa/gold.py` 的 `STAGES` 派生，行数与 θ 为实测值（`unisa train` 末尾那张表）。
> 词表扩张会同时改行数与 θ；`type` 的 9 个类型里 `i16` 是 2026-09-19 补的（E-31）。

**装配方式**（`--drive`，[U-2]）：

| 模式 | 使用的模型 | 合计 θ |
|---|---|---|
| `spec`（默认） | pp lex parse type scope irsel enc reloc **isel abi** | **11,892** |
| `combo` | pp lex parse type scope irsel enc reloc **combo** | 13,584 |
| `gold` | 不用模型，全部查表（训练中途仍可编译，见 [O-1]） | 0 |

`combo` 是 `isel`+`abi` 的**替代**而非追加：同一 key 空间 `op×os×arch` 上的 9 个头合一。
两条路径在完整 gold 上逐 key 同类（[D-7]），所以 `--drive` 不改变编译结果，只改变由谁作答。

---

## 二、版本沿革（v1 .. v3.4）

### 7.3 版本沿革

`archive/prd.v1.md` 原始规格 · `v2.1` 展开版（英文） · `v2.2` 精炼版 · **v3 分层重编号**。

**v2 钉死**（按会先咬人的顺序）：LR 是 epoch 衰减表而非参数量分档 · `h0=52` 给出五段分解 · 9 头连同词表枚举 · `OPS` 固定 38 且顺序写死 · "FULL gold" 定义为完整笛卡尔积 · parse 覆盖规则拆行（v1 原文无法无歧义切词）· type 的 `1/19 illegal` 解读为训练下采样 + 全量评估 · enc/reloc/irsel 默认分支补全 · 补齐 connect/bind/listen/accept 的 win 符号 · r0–r7 寄存器映射与 MOPS 的 `none` 约定 · 完整列出 tape 指令集 · `--fold` 明确为逐目标 lowering **且**目标感知解释 · 加入反向对照 · 补 Mach-O/PE 魔数、退出码、`--drive`、`--holdout` 语义、Oracle 分发 · 选定 fib/ptr/struct 预期输出。

**v2.1 新增**：确定性契约 [D] · 超级拟合确立为目标、`SHIP_ACC=1.000` [F] · q4/q2 dtype 与按行 scale [Q] · argmax 不变性作为量化唯一判据与逐阶段混合 dtype [Q-6..Q-9] · 体积/速度预算与 tinycc 对比 [B]。

**v3 新增**：§1.4 证明义务与接口约束 [P] —— Oracle 不透明性、key 全域性、五层义务分层；全文条款编号；四层重排；验收清单挂条款依赖 [A-5..A-16]。

**v3.1 新增**：§6 实验发现 [E] —— 面向论文的知识沉淀章节，五栏固定格式（命题/证据/机理/意义/状态），已录 E-1..E-5 已证实、E-6..E-11 待验证、E-2′/E-3′ 已证伪、四条开放问题。据实测修正两处：G-2a 的 1/19 下采样已废止（E-2），B-2a 明确体积对比基线（E-3）。并修正 v2 的计数笔误 PRODS 34→32。

**v3.2 新增**（2026-09-19）：§6.6 先行研究与定位 [R-1..R-4] —— 据 `research/prior-art.md` 撤回三条与文献重复的主张（P-8 存在性、P-8b 难度、穷举验证方法），把 P-8a/P-8c 从"开放问题"改回它们本来的名字（两级逻辑最小化），并把体积主张整体换成**接口统一性**。新增外部语料验收 [A-25] 与真内核执行 [A-26]；新增宿主契约 [I-12]（ELF 的 rw 段）；`type` 表补入 `i16`（`short`），TYS 8→9、行数 1216→1539。§3.2 的模型总表改为由 `unisa/gold.py` 派生的实测值。

**v3.4 新增**（2026-09-21）：**§5.5 完成度盘点** [S-1..S-8] —— 四层目标、各自的可测判据、实测数字与卡点排序；**libc 地板** [W-15]（`<ctype.h>` `<limits.h>` `<assert.h>` + `exit`/`abort`）；**多翻译单元** [W-14] [A-30] —— `unisa compile a.c b.c` 无链接器、无目标文件，同一个 walker 走完全部单元；文件作用域 `static` 按文件改名，单文件 tape 逐字节不变。**自举差距上棘轮** [A-29] `tests/selfgap.sh`：unisacc 自己的前端只接受 111/220 语料与 31/73 探针，而这个差距此前**没有任何仪表**（见 E-43）。

**v3.3 新增**（2026-09-21）：**六个目标全部真机执行** —— E-35 改写为已证实，两个根因（`MajorSubsystemVersion` 10.0 把加载器拨到严格路径；tape SP 在 Win64 是 volatile，`winstdh` 必须先于 `spinit`）；[I-16] / [I-17] 改正 —— 撕掉之前“`.reloc` 与 load config 对 arm64 PE 必需”的错误推论，并记下那条方法学教训；`tests/crossnative.sh` 接入 UTM 里的 Windows 11，两个 win 目标每次随套件真跑（虚机不在则跳过）。

---

## 从 prd.md 归档的实验条目（2026-09-23）

这些条目在 `prd.md` 的第 6 章之外**没有任何引用**：它们的结论已经进了当前的数字、
表格与验收条款，留在规格里只是让规格更长。标题与编号原样保留，所以
`grep -n "E-40" prd.md archive/prd-history.md` 仍然找得到它们。

#### E-38　解释器比 CPU 宽容：`.write` 会冲掉 ABI 参数寄存器，而 tape VM 不建模这件事

| 栏 | 内容 |
|---|---|
| **命题** | 给 `printf` 补上宽度/标志/精度（`%5d` `%-5s` `%05d` `%.3s`）时，左对齐那条路径把长度留在 `r1` 里跨过一次 `.write`。**解释器给出正确答案，真机给出错误答案**（`[42     ]` 而不是 `[42   ]`）|
| **机理** | lowering 把 `.write` 变成真 syscall，参数按目标 ABI 放进 `x0/x1/x2`（= tape 的 `r0/r1/r2`），**这三个寄存器在 gate 之后就没了**。而 `vm.py` 把 `.write` 当成一条不碰寄存器的原子指令，于是"跨 write 保住 r1"这种代码在解释器里合法、在 CPU 上不合法 |
| **两处都修** | ① 发射器改成把长度**压栈**而不是留在寄存器里；② **让解释器不再宽容**：`.write` / `.print` / `.sys` 之后把 `r0` 设为返回值、`r1`/`r2` 写成 `0xDEAD5EA1DEAD5EA1`。任何依赖它们的代码现在在解释器里也会立刻坏掉 |
| **意义** | 与 E-33 同一类，但方向相反：那次是**解释器不建模页保护/两操作数 ALU**，这次是**解释器不建模调用约定的破坏性**。规律是一样的——**参考实现的忠实度上限就是它建模了多少机器事实**，而每一条没建模的事实都是一个"本地通过、真机失败"的坑。修法也一样：不是只修发射器，而是**把那条事实补进参考实现**，让它今后自己会抓 |
| **状态** | **已证实**（2026-09-20），固化为 [TP-6] |

#### E-42　语料清零：宽字符串与变长数组

| 栏 | 内容 |
|---|---|
| **结果** | `corpus 220   pass 209   wrong 0   **unsupported 0**   knownfail 11`。外部语料里不再有“前端拒绝”的程序：每一个要么通过，要么在 knownfail 里写明为什么不追（浮点、GCC 扩展、C11、以及 [G-2] 那条设计偏差）|
| **宽字符串** | 源文件按 latin-1 读（一字节一字符），所以 `L"你好"` 到了词法器手里还是 UTF-8 字节串——**只有词法器知道它是一个字面量**，所以解码必须在那里做。token 的 **kind 仍是 `str`**：文法区分不了它们，不该为此给 parse 表的 token 轴加一类；区别在**值**是码点还是字节。`wchar_t` 在所有目标上都是 4 字节（Windows 自己是 2，但我们不调 -W 入口），**否则同一份 tape 就不能 lower 到六个目标** |
| **VLA** | 存储不能在固定帧里，于是在**声明处**从 tape 栈上切；两个隐藏槽存基址与字节数（`sizeof` 于 VLA 是**运行期 load**）。关键是回收：块退出要把 SP 放回去，`break`/`continue` 也要，否则**循环里的 VLA 会把栈吃光**。一遍扫描下没法在块首插指令，但不必：**第一个 VLA 动 SP 之前的 SP 就是入块时的 SP**（其余局部全在固定帧里），就地保存即可 |
| **真正的坑** | `int a[1 && 1]` **不是** VLA。我们的常量折叠器不认识 `&&`/`||`/`?:`/比较，于是把它当成了变长的；而该程序恰好 `goto` 跳过声明处——基址槽永远没被写过，直接访存出错。**“什么不是 VLA”比“什么是”更难，而且错得悄无声息** |
| **意义** | unsupported 归零后，`unsupported` 这个桶从此是一个**真正的警报**：再出现一个，就是新的覆盖缺口，而不是一堆旧欠账里又多了一笔 |
| **状态** | **已证实**（2026-09-21）|

#### E-41　gold 第四次错：移位的结果类型，以及宏参数不该被加括号

| 栏 | 内容 |
|---|---|
| **命题** | `<<`/`>>` 的结果类型被当成了“通常算术转换”。C99 6.5.7p3 说的是：两边各自做整数提升，**结果是提升后的左操作数的类型**——右边多宽都不影响。这是 gold 第四次被实现暴露出错（E-2、E-15、E-16 之后），而 `acc` 全程 1.0000 |
| **宏参数** | 同一个语料程序还抽出了一条更早的错：我们给**每一个宏参数都加了括号**，理由是“走查器没有重扫描”。真预处理器从不加括号，而且加了会**改变语义**：`(T) 1` 在 T 为类型名时变成 `((T)) 1`，那根本不是一个 cast。改成逐 token 原样代入后，69 个探针与 207 个语料程序**一个也没变**——说明那个括号从来没有救过什么，只是在等着坑人 |
| **没改的** | 00200 最终还是 knownfail，因为它紧接着测 `sizeof(expr + 0)`，而 [G-2] 让我们**把 int 算术放在 64 位里算**。那是一条写在规格里的设计决定，不是一个 bug；把它记在 knownfail 里并指回 [G-2]，比假装没看见诚实 |
| **意义** | [P-6] 的又一个计数：**`acc = 1.000` 只能说网络等于 gold，不能说 gold 等于 C99**。能看见这一层的只有参考编译器和别人的代码 |
| **状态** | **已证实**（2026-09-21）|

#### E-40　位域：没有地址的左值，和一个藏了很久的 sizeof 偏差

| 栏 | 内容 |
|---|---|
| **命题** | 位域不是“又一种成员”，它是**一种没有地址的左值**。走查器里的左值一直是一对（ACC 里的地址，`self.lval` 里的类型），位域要求它变成三元组 |
| **做法** | 把 `lval` 改成 property：**任何人设置它都会清掉位部分**，只有字段访问会在设完之后把它装回去。这把“两个字段必须同步”从 38 处约束变成了 5 处（产生左值的地方），而不是每个 `self.lval = None` 都得记得配一句 |
| **读写** | 读：把字段**左移到字顶再移回来**——它自己的最高位落在算术右移会复制的位置上，符号问题就自己回答了（无符号用 `lshr`）。写：读-改-写，并且**赋值表达式的值是回读出来的那个**，不是赋进去的那个 |
| **enum 位域** | 符号性是 implementation-defined。像 gcc 一样：**没有负枚举值就是无符号**。语料 00218 正是专门测这一条的：148 存进 8 位位域，若按有符号读回来就是 -108，switch 会静默走错分支。为此 `enum` 得按 tag 记住“有没有负值” |
| **顺手抓到的** | 结体尺寸**没有向对齐向上取整**：`struct { int a; char b; }` 算出 5 而不是 8。它一直错着，因为**没有一个探针去比过 `sizeof`**；一写位域探针就立刻暴露。这是 E-30 那条经验的另一面：**不被读取的量就不会被发现是错的** |
| **范围** | 读、写、复合赋值、前后置自增、常量与运行期初始化、无名填充与宽度 0 的对齐断点都做了；`&` 于位域按 C99 6.5.3.2p1 拒绝。六个目标 6/6，与 `cc` 逐字一致 |
| **状态** | **已证实**（2026-09-21）|

#### E-39　零散角落扫完：剩下的不是一堆 bug，是三处“用枚举形状代替文法”

| 栏 | 内容 |
|---|---|
| **命题** | 外部语料从 196 推到 **206/220，wrong 0，unsupported 4**。掉下的那一批失败**不是互相独立的**：它们抑制在三个地方，每一处都是我们把一条**递归文法**写成了**形状列表** |
| **一：declarator** | `declarator()` 原本是一串 `if`：“`(*f)(...)`”、“`(*p)[n]`”、“`int ()`”……每发现一种写法就加一条。C99 的 declarator 是**可任意嵌套的**，所以列表永远差一条。改成真递归（每层返回一个 wrapper，由外层喂给它）后，**四个语料程序同时过了**，`abstract_type()` 也不再是第二份重复实现。唯一留下的特例是“**最外层具名** declarator 不吃自己的参数表”，因为那是 [W-3] 的 `after_name` 要决定的事 |
| **二：预处理器的 token 粒度** | `##` 被实现成“把宏体从 `##` 处切开、两半拼起来”。但 `##` 接的是**两个 token**：`A ## B+` 在 B 为空时该得到 `A` 和一个独立的 `+`，切半的写法把它们粘成了 `++`。同一处还缺两件：**参数先展开**（除非它是 `#`/`##` 的操作数）——这才是 `XSTR(x) STR(x)` 惯用法成立的原因；以及 `#x` **刚造出来的字符串也要被保护**，否则 `#define VER 3` 会把 `STR(VER)` 变成 `"3"` |
| **三：初始化器的花括号省略** | 数组长度靠数顶层逗号——但 C99 6.7.8p17 允许聚合元素**从平铺列表里取走自己那份**，于是 `PT a[] = {1,2,3, 4,5,6}` 被数成了六个元素而不是两个。顺手抓到一个潜伏很久的：**尾随逗号也被数成了一个元素** |
| **四：没有链接器不等于没有库** | 一个不 include `<string.h>` 就用 `strlen` 的程序（即大部分真实 C 代码）之前直接被拒。现在前端在发现未定义函数时，把我们自己 `include/` 里定义它的头**追加到源码末尾重编一次**——调用在单元末尾才解析，所以追加就够，用户的行号也不动 |
| **意义** | 与 E-30 同一条经验的继续，但更具体：**自有探针会把“我们实现的形状”误认为“文法”**。外部语料的价值不在于多抳出几个 bug，而在于它让“这里其实有一条递归文法”这件事**变得便宜到可以被看见** |
| **状态** | **已证实**（2026-09-21）|

#### E-36　一个文件两条 ISA：Mach-O universal 已经能跑，两个 slice 都真执行

| 栏 | 内容 |
|---|---|
| **命题** | 「综合多 ISA 可执行体」的第一步不是 cosmopolitan 式的跨 OS 文件，而是**一个 OS 之内**的多架构容器。macOS 的 Mach-O universal 就是它，而且本机两个 slice 都能真跑 |
| **证据** | `unisa fat examples/fact.c -o fact` → `file` 认作 *Mach-O universal binary with 2 architectures*；`./fact` 走 arm64 原生、`arch -x86_64 ./fact` 走 Rosetta，两者都输出 120。`tests/fat.sh` 在 49 个探针上**两个 slice 各跑一遍**，0 不符（[A-28]）|
| **成本** | 40 行：大端 slice 表 + 页对齐拼接。两个 slice 本身就是已有的 `osx/x86_64` 与 `osx/arm64` 镜像，一字未改 |
| **意义** | 这条把「多 ISA」从纸面主张变成可执行文件，而且**验证方式是执行而不是读头部**。同时它划清了边界：这是**一个 OS**内的多架构，cosmopolitan 那种同时是 ELF/Mach-O/PE 的文件是另一个问题，我们还没做（各自的 win 镜像已经能跑，见 E-35）|
| **状态** | **已证实**（2026-09-19）|

#### E-34　同一份字节，Darwin 25 跑得动、Darwin 23/24 崩在 dyld 里

| 栏 | 内容 |
|---|---|
| **命题** | 开发机（macOS 26 / Darwin 25）上 44/44 原生通过的 Mach-O，在 GitHub 的 `macos-14`（Darwin 23）与 `macos-15`（Darwin 24）上**全部 SIGSEGV**。签名有效、`otool` 读得出、dyld 把四个段都映射上了 |
| **诊断** | 猜了三轮都不对，改成让 CI 直接给出事实：`DYLD_PRINT_SEGMENTS` + `lldb --batch -o run -o bt`。栈顶是 **dyld 自己**：`dyld3::MachOAnalyzer::forEachRebase_Relocations`，`EXC_BAD_ACCESS (address=0x48)` |
| **机理** | 我们的镜像是 `MH_PIE`，却既没有 `LC_DYLD_INFO` 也没有 chained fixups。dyld 因此回落到**旧的重定位路径**，去读 `LC_DYSYMTAB` —— 而我们从没发过这条命令，于是 `locreloff`（结构体偏移 0x48）从空指针上读。Darwin 25 的 dyld 提前短路了这条路径，23/24 的没有 |
| **修复** | 发三条**全零**的 `LC_DYLD_INFO_ONLY` / `LC_SYMTAB` / `LC_DYSYMTAB`（+8 字节 NUL 字符串表）。镜像 +152 字节，dyld 改走 opcode 路径、发现无事可做。[I-15] |
| **意义** | 两条。其一：**"在我的机器上能跑"对二进制格式尤其不可信**——加载器的容错随版本变化，而我们手写的头部正好落在容错带里。其二：这个 bug **完全在我们的代码之外**，症状是我们的进程崩溃——没有 lldb 的回溯，任何数量的猜测都到不了 `forEachRebase_Relocations` |
| **状态** | **已证实**（2026-09-19） |

#### E-29　自举阶梯第一级：unisacc.c 的词法器，两种构建方式行为一致

| 栏 | 内容 |
|---|---|
| **结果** | `unisacc.c` = 生成的 core（模型 blob + 整数 kernel）+ 手写的编译器本体。当前本体是**词法分析器，其每一个决策都经 `infer(S_LEX, …)`**——与 Python 前端用的是同一张表、同一个 kernel。<br>**cc 构建**与 **unisa 自己构建**的两个二进制，在 28 个源文件上与 Python 词法器**逐 token 相同**（`tests/selfhost.sh`，固化为 [A-20]） |
| **规模** | `unisacc.c` 880 行；unisa 编译耗时 **0.14 s**，产出 5,328 条指令、**100% 编码**、1.1 MB 镜像 |
| **意义** | 这不是"C 写了个词法器"，而是：**编译器的 C 实现与 Python 实现共用同一份学出来的决策表**，且 C 实现可由编译器自己编译。命题里「换权重即换能力」在两套宿主实现上同时成立 |
| **阶梯** | ① 决策层自编译（E-28）✅ → ② 词法器 ✅ → ③ **预处理器** ✅ → ④ 语法分析器 → ⑤ 发射器 → ⑥ 完整自举 |
| **③ 预处理器** | `#ifdef/#ifndef/#if/#elif/#else/#endif/#define/#undef` 由 `infer(S_PP, …)` 决定，含嵌套层栈与"已取过分支"标记；跳过的行原地涂白，行号与偏移不变。**44 个源文件（含带指令的）两种构建方式与 Python 前端逐 token 相同** |
| **为此补齐的编译器能力** | 数组维度接受**常量表达式**（`macname[MAXMAC * 32]`），含 `+ - * / % << >> & ^ \|`、括号、`sizeof(T)`、enum 常量——此前只接受字面量，unisacc.c 直接编不过 |
| **⑤ 语法分析器 + 发射器** | `unisacc.c` 现在是一个真编译器：递归下降镜像 Python 走查器，每个产生式选择走 `infer(S_PARSE, …)`，产出 tape 文本。**22 个程序由 unisacc 编译、经 `unisa vm` 运行，输出与 Python 编译器逐字相同**（`tests/ccrun.sh`）。覆盖：函数与递归、全局/局部/数组/指针、`if/else` `while` `for` `do-while` `break` `continue`、`+= -= *= /=`、`++ --`、三元、逗号、位运算与移位、enum、宏常量与常量表达式、`printf` 的 `%d %c %s %%`、系统调用内置 |
| **⑦ 完整自举 ✅ —— 不动点达成** | 经典三代判据：`A = cc(unisacc.c)` → `B = A(unisacc.c)` → `C = B(unisacc.c)`，**B 与 C 逐字节相同**（sha1 `31c28069…`，23,864 行）。并且 `U = unisa(unisacc.c)` 产出的编译器编译 unisacc.c 得到**同一份字节**——即 Python 驱动与外部编译器构建出的是**同一个编译器**。固化为 [A-23]（`tests/bootstrap.sh`）|
| **⑥ 自编译 ✅** | `unisacc unisacc.c` 产出约 23,900 行 tape，**该 tape 就是一个可用的编译器**：它编译 `fact/fib/hello/a_arith/a_while/a_str` 全部正确（`archive/tests/gen2.sh`，已由 nativeboot 的 N1 = N2 = N3 取代）。<br>**第二代 = 由第一代编译出的 unisacc**，两代在测试语料上输出一致 |
| **压垮第六级的最后一个 bug** | **全局数组只分配了 8 字节** —— 我为了标记"数组"复用了 `declptr`，于是 `.bss g_src 8` 而非 `1024`，下一个全局 `nsrc` 直接压在源码缓冲区上，第 8 字节起的源码被整数覆写。症状是词法流从第 8 个字符起错乱，极难从上层观察 |
| **④ 基础设施：argc/argv** | 自举要求编译器接受文件名，所以 `argc/argv` 从加载器一路打通：tape 得到 `.argc`/`.argv`；解释器与目标机把参数串物化进内存；原生侧在入口把 loader 交付的 `x0/x1` 存进数据段（Darwin 约定），`argv[i]` 是一次 `LSL #3` 的移位加法加两次取址 |
| **为此修掉的六个缺陷（每一个都是自举暴露的，difftest 探针集想不到）** | ① **全局数组不退化为地址** —— `__read(fd, src, n)` 把源码文本的前 8 字节当指针传了过去<br>② **tape 文本格式把 `;` 当注释** —— 二进制字面量里就有 0x3b，整行被截断（`_strip_comment` 现在是引号感知的）<br>③ **`decode()` 不认 `\xNN`** —— 模型 blob 全成垃圾<br>④ **`es()` 绕过输出重定向** —— 全局初始化器的代码被劈成两半<br>⑤ **函数序言用 r1/r2 当临时寄存器** —— 而它们正是 arg1/arg2，≥3 参数的调用全错<br>⑥ **`char *` 的存储宽度被当成元素宽度 1** —— 只存了指针的一个字节 |
| **状态** | **全部七级已证实。`用 unisacc 编译 unisacc.c 得到等价` 成立。** |

#### E-24　UNS2：构造网络的专用格式，全部权重 2,745 B / 默认路径 1,870 B

| 栏 | 内容 |
|---|---|
| **动机** | UNS1 存稠密张量，而构造网络**不是稠密的**：`W1` 是二值且一个单元完全由"每个 key 字段一个字面量位掩码"刻画，`b1` 可由掩码恢复，`W2` 是稀疏小整数。按张量存等于存一堆零 |
| **格式** | 头 16B `UNS2`；每阶段存 `h0/H/字段长度`，`W1` 为 `H × h0` 位（单元主序）的字面量掩码，每个头存 `H` 位的存在位图 + 每个存在单元的 `(类下标, 权重)`，位宽按该头实际最大值取。**`b1` 不存**。无时间戳、无字典序依赖 ⇒ 字节可复现 [D-5] |
| **实测** | `pp 52 · lex 80 · parse 297 · type 183 · scope 61 · irsel 182 · enc 73 · reloc 44 · isel 382 · abi 500 · combo 875`<br>**全部 2,745 B**；**默认 `--drive spec` 路径 1,870 B** |
| **对比** | JSON 缓存 16,933 B（**6.2×**）· 量化训练 kit 23,640 B（**8.6×**）· 裸 bit-packed 决策表 3,010 B（**1.10× 更小**）|
| **备注** | 本实现只用"逐字段位掩码"一种编码；逐字段择优编码（掩码 / 2-bit tag / 共享字面量表）可再降到约 1,374 B，暂未实现 |
| **状态** | **已证实** |

#### E-23　构造后端已落地：编译器跑在纯整数权重上，且比训练路线快 18×

| 栏 | 内容 |
|---|---|
| **落地** | `unisa build-weights` 构造 11 个整数网络（255 单元）并验证全域精确；`--drive built` 让整条编译器跑在其上。七个样例 **全部 6/6**；三种故障注入仍全部退化 **4/6**（fold 的判别力不依赖权重来源） |
| **外延相等的最强证据** | 训练权重与构造权重编译出的镜像**逐字节相同**（lnx/x86_64、osx/arm64、win/x86_64 各验）。这是 [D-7] 在产品层面的确认：两条路线不是"精度都很高"，是**同一个函数** |
| **速度** | 逐阶段决策吞吐（冷，无 memo），构造 / 训练：`combo 76.6k / 3.3k = 23.0×` · `abi 141.8k / 7.5k = 19.0×` · `isel 329k / 20.4k = 16.2×` · `enc 1.06M / 69.9k = 15.2×` · `parse 670k / 43.6k = 15.4×` … **整体 18.3×** |
| **B-4 的意外结论** | 预算是 Python ≥ 50k dec/s。**训练路线在 `abi`(7.5k) 与 `combo`(3.3k) 上不达标**；构造路线全部阶段 76k–1.29M，**全数达标** |
| **B-5** | 端到端编译 `fib.c`：训练 0.006 s / 构造 **0.001 s**，均远低于 1 s |
| **意义** | 构造路线不是"精确但慢而大"的学术选项——它在**精确性、体积、速度**三项上同时优于训练路线，唯一的代价是构造耗时约 6 s（结果可缓存，且确定性） |
| **状态** | **已证实** |

#### E-20　构造与训练互有胜负，最小性是真正的开放问题

| 栏 | 内容 |
|---|---|
| **命题** | 精确解可由构造直接给出（P-8），11/11 全部验证通过、零训练零随机；但**训练得到的解普遍比构造小得多**，两者互为三明治的上下界 |
| **证据** | 逐阶段 θ（训练 / 构造 / 比值）：<br>`pp 274/224 0.8×` · `reloc 161/63 0.4×` · `scope 479/513 1.1×` · `lex 418/990 2.4×` · `isel 2328/18624 8.0×` · `irsel 953/7830 8.2×` · `enc 829/9506 11.5×` · `parse 1288/15456 12.0×` · `combo 10053/447219 44.5×` · `type 801/37597 46.9×` · `abi 4361/258468 59.3×`<br>合计 21,945 / 796,490 |
| **机理** | 表小则构造更省（无需 embedding 与隐层的连续参数化开销）；表大则 SGD 大幅胜出，因为梯度下降会**发现跨规则的共享结构**，而基于规则的构造逐条落项、不共享 |
| **意义** | 修正了一个容易犯的叙述错误：不能说「构造优于训练」。正确表述是**两者各自给出精确解集的一个可行点，且位于最小性谱系的两端**。真正的贡献点因此从「能否 100%」（平凡，P-8）转移到「**最小的精确解有多小、能否确定性地构造出来**」（P-8a/b/c） |
| **状态** | **已证实** |

#### E-4　确定性可完全达成，且代价为零

| 栏 | 内容 |
|---|---|
| **命题** | 两次独立训练产出**逐字节相同**的 11 个权重文件；net 与 gold 在全部 **2,300 个 key** 上零分歧 |
| **证据** | `train --out /tmp/w1; train --out /tmp/w2; diff -r` → 11 files identical（`parse.f32.unisa` sha1 `02f4a8b9…`）。Oracle 双驱动对拍 2300 keys / 0 mismatch |
| **机理** | 随机性只存在于 mulberry32 的初始化与 `seed^epoch` 的 batch 置换；gemv 累加顺序固定；推理无 RNG、argmax 平局取最小下标 |
| **意义** | 反驳"神经网络不可用于编译器因其不确定"的通行直觉。不确定性来自**工程选择**（并行归约、非确定性 kernel、采样解码），不是神经网络的固有属性 |
| **状态** | **已证实**（D-6 / D-7） |

#### E-12　表乘性增长 / 网络加性增长，交叉点在 **128 目标**；但固定宽度下容量不守恒

| 栏 | 内容 |
|---|---|
| **命题（前半，成立）** | key 空间按目标数扩张时，裸表体积**乘性**增长，网络参数**加性**增长；实测交叉点 **128 个目标**（表 18,848 B vs 网络 17,354 θ），480 目标时网络已小 **3.13×** |
| **命题（后半，证伪）** | 但**同一套架构在 24 目标 / 912 行上只到 0.4419**，不到 1.000。加性 θ 增长若以精度崩塌为代价，则前半无意义 |
| **证据** | 合成目标扩张探针（`scratchpad/scale.py`，额外目标为结构派生的**合成**目标，仅用于测参数规模，不声称其正确性）：<br>`6→0.09x  24→0.29x  64→0.74x  96→0.92x  128→1.09x  192→1.62x  480→3.13x`<br>定宽训练：`6 目标 1.0000 / 12 目标 1.0000 / 24 目标 0.4419` |
| **机理（推测，待验证）** | `sysno` 头的类数随 os 数**线性**增长（≈ 19×\|os\|），且该映射本身无结构可共享——本质是纯记忆。头矩阵 `h_last × \|vocab\|` 因此持续变大，同时隐层宽度没变，导致容量先于参数量见底 |
| **意义** | E-3 的软肋（网络打不过裸表）**在大 key 空间上可翻转**，但必须补上一条真正的规模律：**达到 1.000 所需的 θ(n)**，而不是固定宽度下的 θ。这才是论文该给的曲线 |
| **状态** | **部分证实 + 部分证伪**；细化见 E-13 |



## J10 全文（2026-09-25 从 prd.md 移出，J10 已暂停）

**J10. 自我迭代：权重的构造离开种子（2026-09-25，主人：“接下来应该推动 unisacc 的自我迭代进化”）**

判据：种子层（Python）完结，不是看代码是否还在，而是看迭代层能否独立走完“表 → 权重 → 编译器 → 自身”。现在权重只能由 gold.py 加 construct.py 产出，种子一旦冻结，权重也就冻结了。

| 步 | 内容 | 判据 | 状态 |
|---|---|---|---|
| 1 | .tsv 自带 schema（字段取值和头类别，按顺序）；答案只取自 .tsv，不再用 gold.py 的规则计算 | `build-weights --from-tsv --check`：UNS2 原始字节与发布版相同，JSON 缓存按 save_all 写出后与发布文件原始字节相同，缺失或不同都判失败；输入契约拒绝损坏的表（`tests/tsv_contract.py` 五种破坏全部拒绝）；`tests/tsvbuild.sh` 已登记进 all.sh | **已达**（ee74b8b、8d4011c），18 个阶段，9,124 B，Python 约 20 s。**尚未物理脱离 gold.py**：阶段清单与顺序仍取自 gold.ALL，Stage 仍借用 gold.Stage 作容器；C 入口不得暗中依赖这两点，要么由数据声明，要么显式列出 |
| 2 | 用 unisacc 能编译的 C 子集实现构造器（读 .tsv，写 UNS2），由 unisacc 自己编译 | 18 个阶段的 UNS2 与 Python 逐字节相同；构造器由 unisacc 自编译后结果也相同；每次运行不超过 60 s | **进行中**，当前实况（2026-09-25）：已移植 10 个阶段（prec、reloc、tyinfo、regmap，验收批次 pp、lex、scope、pfconv、binsel，以及 enc）（多头路径只在 tyinfo 上验证过；分解候选只在 regmap 上验证过，见下方分支记录）；C 侧已有 UNS2 写出器，三个阶段的 UNS2 原始字节与 Python 参考相同，各节也与发布的 built.uns2 相同；部署语义往返走的是 Python 的 UNS2 解码器（C 的 MODEL 内核部署未验证）；部署不变量及其负例（-T bias / -T act）已覆盖；uns2.py 的格式说明已修正；只在 osx/arm64 上测过。其余 8 个阶段未移植，受限情况见下方矩阵。*历史*：第一片 06be1d6 只做了 prec、reloc 的规范转储，当时尚无 UNS2 写出器、多头未移植、不变量没有负例 |
| 3 | 迭代层生成 ckernel.py 的**全部**产物，而不只是 DENSE：UNS2 不带字段值和类别名称；C 模型的阶段顺序来自 ALL，而 UNS2 用 sorted(nets)；MODEL 块、词表、维度、词法辅助表、ENCSPEC、头文件与自测用例各有来源。先列一张“产物 ← 来源（TSV / UNS2 / 其余声明数据）”的对照表，再逐项迁移 | 生成的内核文件与 ckernel.py 的输出逐字节相同；`--check-oracle` 差异为 0 | 待做 |
| 4 | 闭环，先限定为 **schema 不变的表值修改**（不声称任意扩表已闭环）：在隔离环境里真实改一张 .tsv，只用 unisacc 走完“构造 → 内核 → 编译器 → 自身”，全程无 Python | 最终产物随这处修改而改变（不动点本身不能排除偷偷沿用旧权重），并且 N1 = N2 = N3；Python 只作为冻结的第二实现做孪生检验 | 待做 |

工程边界：构造器是迭代层的**独立工具**（`iterate/construct/`），由 unisacc 构建、单独运行，不进编译器每次启动的热路径；ARCHITECTURE.md 在第 2 步落地时相应标注。
第 2 步的对齐方法：先固定序列化和 tie-break 规则，沿“schema → 商化分组 → cube 与排序 → 权重 → UNS2”找**首次分歧**，不只反复比较最终 blob；字节一致、自举一致、全域语义一致（原始全部键、每个输出头与 TSV 逐项核对）分别记账。
**第 2 步发现的 unisacc 误编译（都已确认并修复，C 前端；Python 前端一直是对的）：**
  1. 三维数组用变量下标时段错误：下标表达式会清零内层维数，P_INDEX 却在求值之后才读取它。已在 2976a43 修复；探针为 `tests/c/a_arr3v.c`。
  2. 调用结果沿用了最后一个实参的指针性：`-(f(rows[i]) - 1)` 里的 1 被当作指针偏移乘了 8（构造器的 b1 因此变成 7 而不是 0）；返回指针时，`*(ip() + 2)` 按字节步进。第一次缩小时实参不是指针，所以没有复现。已在 7e32309 修复（`callptr` 按函数声明设置结果）；探针为 `tests/c/a_callres.c`；构造器已恢复成单一表达式。
两项都说明逐键的语义核对不够：误编译出的网仍然通过了逐键验证，是与 Python 的逐字节比较抓出来的。
（每字段商化组数也已实测：字段组集合是单个 long，上限 62，由 cedd477 起在 domain() 里显式检查。）
（规则数与单元数用 Python 参考逐阶段实测，2026-09-25：每头最多规则数 / 网络单元数。MAXR 62 是继商化键之后的下一个共同上限。）
（整包闭环另列：18 个阶段各自通过不等于整包通过；MAXS=8、阶段排序、头部总量与跨阶段的状态清理，要在之后一个独立的整包步骤里验收，目前只登记、不扩 MAXS。分批检查四批合计 58.1 s，这是分批总量；单批上限仍是 60 s，不因总量接近 60 s 而提高。）
**C 构造器的阶段限制矩阵**（上限：MAXF 3 个字段，MAXH 16 个头（abi 起，原为 4），MAXV 每字段 128 个取值（059c506 起，原为 62），MAXC 96 个类别（irsel 起为 2 个字，第二字 34 位；此前 62，0723641 起，原为 32；类别集合仍是单个 long，位 ≤ 61），MAXOK 4,352 个原始键（ad0585f 起，原为 4,096），MAXQ 3,200 个商化键（ad0585f 起，QW = (MAXQ+QB-1)/QB = 52；此前为 1,024，cedd477 起为 17 个 62 位字；原先的 16 个 long 的位集，原为一个 long、62 个）；MAXR 每张决策表 62 条规则；另外一个 `-u` 包最多 MAXS 8 个阶段，所以即使解除多头和位集两项，也不能一次写出全部 18 个阶段）：

| 阶段 | 字段 | 头 | 各字段取值数 | 最多类别 | 原始键 | 商化键 | 受限于 |
|---|---|---|---|---|---|---|---|
| prec | 1 | 1 | 19 | 11 | 19 | 11 | 无（已移植） |
| reloc | 2 | 1 | 3/2 | 3 | 6 | 4 | 无（已移植） |
| pp | 2 | 1 | 9/2 | 4 | 18 | 8 | 无（已验收，ad5c5aa） |
| lex | 2 | 1 | 12/12 | 10 | 144 | 40 | 无（已验收，ad5c5aa） |
| scope | 2 | 1 | 6/5 | 7 | 30 | 24 | 无（已验收，ad5c5aa） |
| regmap | 2 | 1 | 8/2 | 16 | 16 | 16 | 无（已移植，2bd6a02） |
| pfconv | 1 | 1 | 9 | 7 | 9 | 7 | 无（已验收，ad5c5aa） |
| binsel | 2 | 1 | 16/2 | 23 | 32 | 32 | 无（已验收，ad5c5aa） |
| tyinfo | 1 | 3 | 16 | 4 | 16 | 10 | 无（已移植，72556aa） |
| opinfo | 1 | 3 | 67 | 16 | 67 | 18 | 无（已验收，ebdd5ae；代码未改，MAXV 放宽后即在容量内） |
| enc | 3 | 1 | 73/3/2 | 5 | 438 | 8 | 无（已移植，059c506；MAXV 已放宽到 128） |
| peep | 3 | 1 | 8/17/12 | 10 | 1,632 | 528 | 无（已移植；商化键集合为多字位集，每字 62 位、QW 17，MAXQ 1024；cedd477 修正了未定义行为并补回字段组上限） |
| type | 3 | 1 | 15/19/15 | 16 | 4,275 | 3,150 | 无（已移植，97f61f7；MAXOK 4,352，MAXQ 3,200；规则 62 正好用满 MAXR） |
| parse | 2 | 1 | 5/68 | 36 | 340 | 145 | 无（已移植，0723641；MAXC 放宽到 62） |
| irsel | 2 | 1 | 6/70 | 70 | 420 | 414 | 无（已移植，3f953aa；字段组 GW 2 字、MAXG 124；类别 CW 2 字、MAXC 96） |
| isel | 2 | 2 | 73/2 | 94 | 146 | 138 | 无（已移植，cd17f13；MAXR 96） |
| abi | 3 | 13 | 73/3/2 | 62 | 438 | 144 | 无（已移植，aa521ee；MAXH 16，MAXR 80，MAXCAND 288） |
| combo | 3 | 15 | 73/3/2 | 94 | 438 | 414 | 无（已移植，f73d3cf；MAXR 96，MAXU 200，MAXCAND 432，候选行宽 MAXCU = MAXR） |

**tyinfo 已达**（72556aa，独立复跑通过）：规范转储 2,657 B，UNS2 单阶段包 104 B 与 Python 参考相同，其中 tyinfo 一节 88 B 与 built.uns2 相同；16 键 × 3 头的部署往返无并列；不变量按共享的单元检查，一次覆盖全部头；cc 与 unisacc 两种构建都通过。**实际走到的分支**：三个头都只有一个候选（dlist；单字段阶段没有分解候选）；pick 从 4 个起点出发，没有换过任何头；T4 跑满 3 轮，接受 9 张表，其中 3 张有变化，拒绝 0 张；REDUCE 对齐池中立方体 4 次，池标志从未改变选择；合并了 2 个立方体（候选合计 8 个单元，网络 H=6）。**未走到的分支**：多字段阶段的分解候选及其下游全部分支（pick 选中或换选分解候选、kind 为 factored 的头）；T4 拒绝更长的表；T4 提前结束；池标志打破并列；起点并列；4 个头。所以多头算法只在这一条路径上得到验证。
**regmap 已达**（2bd6a02，独立复跑通过），第一个多字段阶段：规范转储 1,097 B，UNS2 单阶段包 99 B 与 Python 参考相同，其中 regmap 一节 83 B 与 built.uns2 相同；16 键的部署往返无并列；不变量及其负例通过；cc 与 unisacc 两种构建都通过。**实际走到的分支**：partitions_of(2) 只有一个划分；rep_factored 调用 18 次（merge 取 2 个值 × k 取 0..8），没有返回 None，也没有因为不比 dlist 短而被丢弃，18 个全部保留；选中的是候选 1（factored，merge 0，k 0，10 个单元；dlist 为 16 个单元），pick 从全 dlist 的起点换到了它。**仍未走到**：rep_factored 返回 None；候选因不更短而被丢弃；补丁单元；多个划分和 3 个字段；以及只有一个头所以不涉及的 T4 与跨头合并。
**验收批次 pp、lex、scope、pfconv、binsel 已达**（ad5c5aa，独立复跑通过，check.sh 全程 3.9 s）：现有构造器一行未改，五个阶段在 cc 与 unisacc 两种构建下，规范转储、UNS2 单阶段包与发布节、全域逐头唯一 argmax、部署不变量全部通过（转储 589 / 1,086 / 942 / 555 / 1,755 B；发布节 58 / 93 / 68 / 57 / 141 B）。**首次走到的分支**：rep_factored 返回 None（pp 12 次，lex、scope 各 18 次，这三个阶段都选 dlist）；binsel 选中 factored（18 个单元，dlist 为 23），18 个候选并列、按下标先到者胜。pfconv 只有一个字段，不走 T5。**仍未走到**：候选因不更短而被丢弃、多个划分、3 个字段、tyinfo 之外的多头；补丁单元在追踪里看不到。**整包**：9 个阶段超过 MAXS=8，没有扩，只用单阶段包验证，不声称 9 阶段整包已通过。
**enc 已达**（059c506，独立复跑通过，check.sh 全程 4.1 s），第一个 3 字段阶段：先审计 MAXV 的全部用法和全部位运算——没有任何移位或掩码以原始取值为下标（原始取值先映射到商化组再移位），商化组、商化键、类别、rank 的位集都有各自的上限检查；所以只把原始取值的存储从 62 扩到 128。规范转储 1,510 B，单阶段包 115 B，发布节 99 B 与 built.uns2 相同，438 个原始键的部署往返无并列，不变量及其负例通过；cc 与 unisacc 两种构建都通过。**分支**：4 个划分（首次多于一个），rep_factored 调用 40 次全部返回 None，选 dlist（5 个单元）。**容量门禁**：商化键超过 62 时改为以退出码 4 报“capacity: N quotient keys so far, more than 62”（原先是 1）；新负例用 9×7 取值、63 个商化键的合成表，两种构建都在任何移位之前以 4 拒绝，check.sh 只接受退出码 4 加上该诊断。MAXQ、MAXC、MAXH、MAXS 都未动。
**peep 已达**（商化键位集），第二个 3 字段阶段：事前审计——Python 参考上 peep 有 18 条规则、最大 rank 3、1 个候选（72 次 rep_factored 全部返回 None）、选中与候选均 18 个单元、4 个划分，都在 MAXR、MAXU、MAXCAND 等上限之内，唯一的阻挡是 528 个商化键 > 62。只把商化键集合（qmask、ALL、cubemask 结果、expand、labmask、rem、bad、newly、ranks 与 headfail 的键集合、covered、resid）改为 QW=16 个 long 的位集，MAXQ 62→1024；字段内的组集合、类别集合、rank 权重都不动。差集一律再与 ALL 相与，不产生域外的键；迭代按升序，与 Python 的 range(D.n) 相同；`construct -Q` 自检 63、64、127、128 与末键。静态内存 __common 4,659,224→9,181,848 B（+4.5 MB，bmem[MAXQ][MAXQ] 占 93%，全是 bss）。cc 与 unisacc 两种构建：规范转储 2,727 B，单阶段包 178 B，发布节 162 B 与 built.uns2 相同，1,632 个原始键部署往返无并列，不变量及负例通过；原 11 个阶段全部不变。**分支**：与 enc 相同（4 个划分，72 次全 None，选 dlist），没有首次走到的分支。**容量门禁**：原 63 键负例改为正例——原标签需要 63 条规则，超过 MAXR 62（另一项上限，未放宽），换成仍然 63 个商化键、只需 15 条规则的标签；两种构建都构造成功、全域验证通过，并与 Python 参考逐字节相同。新负例 11×11×9=1,089 个商化键，两种构建都以退出码 4 拒绝。
**peep 的位集修正**（cedd477，独立复跑通过，含我亲自跑的 UBSan）：评审用 `cc -O1 -fsanitize=undefined` 查出 3143a5a 的两处缺陷——`((long)1)<<63` 是未定义行为（popc 的 `m-1`、负数右移同样有风险）；商化键上限放宽到 1024 后，字段组数不再受它保护，而字段组集合仍是单个 long。修法：每个字只用 62 位（QB 62，QW 17 = ceil(1024/62)），所有值都在 [0, 2^62)，只用有符号 long 已测过的编译路径；README 的 UB 审计表逐项列出 14 类移位、减法、计数与迭代。domain() 在任何移位之前检查每个字段 ng ≤ 62（组位最大 61，bit(62)-1 可表示），越界以退出码 4 报“field F has N value groups”。门禁：UBSan 构建跑 -Q（10 个规模，含 61/62/123/124/1023/1024 等字边界）和 12 个阶段的转储与 UNS2，全部干净；新增字段组负例（70×2 取值、140 个商化键、字段 a 有 70 个组），cc、unisacc、UBSan 三种构建都以 4 拒绝；原有商化键负例与 63 键正例保留。12 个阶段的全部字节不变。未在 UBSan 下跑：分支追踪、读取器负例、不变量负例、63 键正例。
**parse 已达**（0723641，独立复跑通过，含我亲自跑的 UBSan）：先审计类别集合的全部位运算（gcls/bcls、rankset 的 s>>c 与 bit(crank)、cmpset、wmask>>i；labmask、cw、W2 等只是按类别下标的数组），最高位都是 ncl-1；以 rank 为下标的移位与类别数无关（lv ≤ 60，k < 9）。据此 MAXC 从 32 扩到 62（ncl ≤ 62 时最高位 61，不碰符号位），在读 #head 行时、任何类别移位之前检查类别数，超过则以退出码 4 报“head H has N classes, more than 62”。parse 其余各项对照 Python 参考都在上限内。规范转储 4,305 B，单阶段包 559 B，发布节 543 B，340 键往返无并列，不变量与负例通过；cc、unisacc、UBSan 三种构建都通过。分支：3 个候选，选中 factored（34 个单元），rep_factored 18 次中 16 次返回 None、保留 2 个——首次在同一阶段里同时出现两种结果，但没有新分支。新负例：63 个类别的 2×2 表，三种构建都以 4 拒绝。
**type 前置审计**（2026-09-25，只读，未改代码；草稿原型在 scratchpad）：Python 参考上 type 的决策表 62 条规则，正好等于 MAXR 62（C 在加规则之前判断 nr ≥ MAXR，所以放得下，但已无余量）；最大 rank 7；4 个划分，rep_factored 72 次全部返回 None；只有 1 个候选（dlist）；H = 62；build_net 耗时 2.5 s（其中 decision_list 2.1 s）。**阻挡**：原始键 4,275 > MAXOK 4,096；商化键 3,150 > MAXQ 1,024；以及一处语义差异——被拒的分解候选在构造过程中单元数会冲到 245（加补丁到 2,969），C 在 newunit 达到 MAXU 时直接中止，而 Python 在单元数达到上限时只返回 None。**bmem**：bmem[x][i] 存组值编码，行 x 是“能到达的类别集合相同”的桶；由 rep_factored 按首次出现顺序写入，读之前按桶内排序与桶序（border）重排，所以写入顺序不影响结果。每个组值恰好进一个桶，所有行成员总数 = ngv ≤ nq；插桩在 13 个阶段加 type 上实测，违例 0。可等价压平为一维成员池 bpool 加行偏移 boff、行长 bn（先计数、再求前缀和、再按原顺序填入）。**内存（静态，非 RSS）**：现在 4,676,360 B，其中 bmem 4,194,304 B，压平后为 494,344 B；扩容到 MAXOK 4,352、MAXQ 3,200、QW 52 后，不压平 42,292,128 B（bmem 约 41 MB），压平后 1,370,528 B。**建议顺序**：一、在现有容量下单独压平 bmem，13 个阶段逐字节不变；二、在 rep_factored 的两处 newunit 之前加“单元数达到上限就返回 0”，与 Python 返回 None 等价；三、扩 MAXOK 4,352、MAXQ 3,200、QW 52，并改掉注释里写死的 1024/17；四、把 type 加进 check.sh。**MAXR**：已被 type 用满。之前写的“规则更多就要改规则位宽”证据不足：cons 是矩阵，fire 存的是规则编号，位权取决于 lv 而不是规则下标。所以规则条数与 rank 要分开审计；以后扩 MAXR 可能涉及数组、循环和权重和的溢出，但不能把“规则数 > 62”直接等同于位宽不够。
**type 已达**（四步，各自一个提交，独立复跑通过，check.sh 全程 32 s）：
一、b95a104 在现有容量下压平 bmem 为一维成员池 bpool 加按桶身份索引的 boff、bn；断言成员总数 = ngv ≤ nq 且每桶填满；13 个阶段在 cc、unisacc、UBSan 下与改前快照逐字节相同；静态 __common 12,294,848 降到 8,100,544 B（静态空间，非 RSS）。
二、9fc94f7 在 rep_factored 的基础分解块与补丁两处 newunit 之前，单元数到 cap = nr 即返回 None：保留的是候选的结果，不是被拒候选的中间单元轨迹；入口检查 nr > 0 且 nr ≤ MAXU，否则退出码 4。-t 分列基础与补丁路径的提前拒绝次数（pp 6/3，reloc 6/0，lex 18/0，scope 18/0，enc 38/0，peep 72/0，parse 14/0）；regmap、binsel、parse 选中的 factored 候选不变，没有误杀。
三、ad0585f 扩容：MAXOK 4,352，MAXQ 3,200，QW 派生为 52；原始键超限改为表头处的容量检查（退出码 4，先于任何索引）；-Q 增至 13 个规模，覆盖新的尾字；新负例 15×15×15（3,375 个商化键）与 67×66（4,422 个原始键）都以 4 拒绝，且确实先到各自的检查；静态 __common 增到 8,953,912 B。
四、97f61f7 type 验收：规范转储 8,933 B，单阶段包 593 B，发布节 577 B，4,275 键往返无并列，不变量与负例通过；cc、unisacc、UBSan 都通过。分支：4 个划分，rep_factored 72 次全部返回 None（提前拒绝：基础 54、补丁 18），只有 dlist 一个候选，62 条规则，H = 62。耗时：netdump.py 7.1 s，uns2slice.py 2.6 s，construct 在 cc/unisacc/UBSan 下 0.19/2.25/1.39 s。
**未覆盖**：rep_factored 入口的前置检查（nr ≥ 1 且 MAXR 62 < MAXU 128，打不到）；桶池的两条内部断言（由构造保证）。
**abi 前置审计**（只读，未改代码）：Python 参考上 52 张决策表（13 头 × 初始加 3 轮 T4）中 sysno 67 条规则，其余 3 到 13 条；最大 rank 为 5，离 lv ≤ 60 很远。T4 跑满 3 轮，每轮接受 13 张、拒绝 0 张。只有 sysno 有分解候选，每次 72 次调用保留 64 个，T4 每轮重新追加，所以候选总数在 4 次选择时依次为 77/141/205/269；C 的 ncand 是全局计数、每次尝试前检查，需要 MAXCAND ≥ 270。rep_factored 调用 2,208 次，1,952 次返回 None；Python 的中间单元峰值 135 超过 128，但 C 在 cn ≥ nr 时提前返回，提前拒绝 1,940 次（基础 1,916，补丁 24），与 Python 完全一致，所以 C 每个候选的峰值 ≤ nr = 67。pick 的 total_units 峰值 105 < 128（单元余量最紧的一处）；最终选中 sysno 的 factored 候选（33 个单元），其余各头 dlist，共 97 个单元，共享后 H = 50。build_net 2.25 s，netdump.py 4.58 s。**C 侧**：规则上限的唯一检查是 decision_list 里加规则前的 nr ≥ MAXR，所以 67 是精确最小值（66 实测中止）；所有循环都跑到 nr；rank 位权由 lv 决定，与规则下标无关，扩 MAXR 不改变任何位宽；但各处 logit 累加（ranks、rep_from_dl、headfail、验证器）没有溢出保护，最坏约 nr·2^lvmax：7×2^60 仍小于 2^63−1，8 条 rank 60 的规则同时命中一个键才会越界。已批准的做法是在每次加法前检查 a > LONG_MAX − b，而不是人为压低 rank 上限。头数上限在读取器检查（13 通过，12 中止）。候选相关的 ccube、cw 按 ci·MAXU+u 分行，每多一个候选多 66,560 B。**草稿验证**：只改 MAXH 13、MAXR 67、MAXCAND 270，abi 的 -d 与 Python 相同（45,129 B）；改 16/80/288 时，cc、UBSan、unisacc 三种构建都相同，UNS2 1,544 B 与参考相同，发布节 1,528 B 相同，438 键 × 13 头往返无并列，14 个阶段不变。**静态内存**（__common，非 RSS）：现在 8,953,912 B；16/80/288 为 23,175,224 B，其中 MAXCAND 一项就增加 11.6 MB。**建议顺序**：一、MAXR 62 → 80，并处理 logit 累加的溢出；二、MAXH 4 → 16，用多于 4 个头的合成表验证；三、MAXCAND 96 → 288，或者先像 bmem 那样把 cw、ccube 压平成单元池；四、把 abi 加进 check.sh。
**check.sh 分批**（已合并，独立复跑通过）：阶段清单是脚本顶部的一张表，所有循环都读它；可用 STAGES 选择阶段，GLOBAL 开关与阶段无关的全局检查；空选择、未知阶段、只关全局检查都以退出码 2 失败；末尾打印本次跑了哪些阶段、哪些全局检查执行或跳过、每个阶段正负例是否执行。`--batches` 把完整清单分三批（前 7 个阶段加全局检查 3.9 s；中间 6 个阶段 4.0 s；type 单独 27.7 s），每批在 alarm 60 下运行，运行前核对各批的并集正好是全部阶段加全局检查、各一次。失败会传播：用一个坏掉的编译器跑分批模式，三批都报 rc 1，整体 FAILED、退出码 1。
**abi 已达**（五步，各一个提交，独立复跑分批模式通过：四批 5.8/4.0/28.5/12.7 s，每批在 alarm 60 下）：
一、受检累加：唯一的检查函数 ladd(a,b) 先确认 a、b 非负（main 另查 long 为 64 位），在相加之前判断 a > LONG_MAX − b；接管全部 4 处累加（ranks、rep_from_dl、headfail、验证器）；溢出以新退出码 6 报错；check.sh 静态核对恰好 4 处调用 ladd、没有未受检的累加。-T summax 以 0 退出，-T sumover 与 -T sumrun（7 个 2^60 通过、第 8 个被拒）以 6 退出，cc、unisacc、UBSan 都通过，UBSan 无报告。
二、MAXR 80：加规则前的检查改为容量退出码 4；63 条规则的 9×7 合成表转为正例（-d 4,461 B、UNS2 285 B 与 Python 相同）；81 条规则的负例以 4 拒绝；total_units 上限也改为 4；main 检查 MAXR ≤ MAXU。
三、MAXH 16：读取器头数检查改为 4；6 个头的合成正例（-d 6,725 B、UNS2 188 B 与 Python 相同，首次触发“池标志改变规则选择”）；17 个头的负例以 4 拒绝。
四、MAXCAND 288：候选池表示不变；factored() 的检查改为 4；实测 269 时 abi 以 4 退出、270 可以构建，270 只是 abi 在当前构造顺序下的最小需求，不是通用上界；219 槽的正例（regmap 头复制 3 份，首次覆盖多头加 factored、pick 换头）与 289 槽的负例。静态 __common 23,175,224 B；abi 峰值 RSS 13,893,632 B（cc 构建，0.09 s）。
五、abi 验收：规范转储 45,129 B、UNS2 1,544 B、发布节 1,528 B 都相同；438 键 × 13 头往返无并列；不变量与负例通过；cc、unisacc、UBSan 都通过。分支：rep_factored 2,208 次，1,952 次返回 None，保留 256 个；提前拒绝基础 1,916、补丁 24；4 次选择的候选数 77/141/205/269；pick 移动 52 次；T4 3 轮接受 39 张、拒绝 0 张；H = 50。
**未覆盖**：ladd 的负加数分支、long 不是 64 位、MAXR > MAXU、total_units 的退出 4、share 池超过 MAXU、rep_factored 入口检查、桶的内部断言、T4 拒绝与提前结束。
**分批回执**（2211a62 合并后的提交，独立复跑通过）：此前的 --batches 只核对计划、执行后只看 rc，summary 按选择生成，失败时也会打印 positive yes——那是尝试清单，不是回执。现在每项检查只在成功处写通过标记；阶段要在它的全部必需标记（各构建的转储、UBSan、各构建的 UNS2、发布节、往返，以及适用的追踪与不变量负例）都齐了才发回执，全局检查同理；子进程最后逐行输出实际通过的回执，已尝试却无回执的项也让子进程失败；父进程核对每批回执与请求完全相同（无缺、无多、无重复），并核对并集等于 15 个阶段加 14 项全局检查、各一次；rc 非零仍立即失败。summary 分列尝试、通过、失败、跳过。故障证据：子代理在临时副本里让 peep 漏掉 UNS2、漏掉 capc，父进程都因缺回执失败（rc 为 0 时也失败），坏编译器仍走 rc = 1 路径；我另在临时副本里去掉“发布节通过”标记，单独跑 pp，得到“attempted, FAILED, no receipt; missing [s pp shipped cc] [s pp shipped ua]”。正常构建下四批 5.1/4.2/28.0/12.8 s，回执 21/6/1/1，并集 29 张。
**irsel 集合语义审计**（只读，未改代码）：Python 参考上 irsel 只有 1 张决策表，75 条规则，最大 rank 0；1 个划分，rep_factored 18 次全部返回 None，只有 dlist 一个候选；H = 75；build_net 0.12 s。阻挡只有两项：字段 2 有 69 个组、头有 70 个类别；其余各维都在上限内。现在 C 在读取器里就因类别超限以 4 退出，还没到 domain()，所以“只迁字段组”这一步无法用 irsel 验证。**cmpset** 取 a^b 的最低位 p，按“谁在 p 之上还有成员”判先后，这正是 Python 对升序成员列表的字典序比较（对应 betterthan 的 cube_key 平局判定、share 池的 sorted(pool, key=cube_key)、桶排序的 sorted(map(str, s))，后者经 crank 保持类别名顺序）；多字版本必须从低字向高字扫描，按字典序比较，不能按多字整数大小比较（反例：{0,62} 与 {1}）。**复用**：qset、qtest、popc 只依赖参数，可以复用；其余 q* 函数读全局 nqw、ALL、nq，要改成带显式字数与尾部掩码的通用函数，q* 退为薄包装；数组保持扁平二维。**静态内存**（size -m 实测）：现在 23,175,224 B；字段集合用 2 个字 24,095,800 B（其中 ccube 随 MAXCAND × MAXU 放大约 0.88 MB）；再把类别集合用 2 个字、MAXC 124，就到 43,461,688 B，几乎全部来自按类别下标的 cw（每个类别 294,912 B）与 W2，类别集合本身只增 51 KB；MAXC 96 足够覆盖 isel、combo 的 94 个类别，约增 10 MB。**顺序**：确认“先辅助函数，再字段组，再类别，再 irsel”。第 0 步加 -G 自检（字数 1 到 3、跨字边界、空集与全集、andnot 后的尾部、字典序比较与暴力比较一致）；第 1 步字段组迁移后，70×2 的字段组负例转为正例并与 Python 比对，另造新上限之外的负例；第 2 步类别迁移后，63 类负例转为正例，另造新上限之外的负例，并**必须另造一个会保留 factored 候选、可达类别集合跨越第 61/62 位、类名顺序与下标顺序不同的合成正例**，因为 irsel 本身的字节一致测不到类别比较；第 3 步 irsel 验收。待定：字段组上限用 2 个字（124）还是 3 个字（与 MAXV 128 相同，使组数检查成为冗余）；MAXC 取 96 还是 124，或者先压平 cw。
**irsel 已达**（四步，各一个提交，独立复跑分批模式通过：四批 7.6/5.8/29.7/15.0 s，回执 25/7/1/1，并集 34 张 = 16 个阶段 + 18 项全局检查）：
〇、8f51ca4 ws_ 通用集合函数（显式元素数、活动字数、存储字数、末字掩码；未用字清零；零域有定义；字典序比较从低字向高字扫描），q* 退为薄包装、只读写前 nqw 个字以保持原语义；-G 自检覆盖 8 个域长（0、1、61、62、63、96、123、124），2,847 次与独立成员列表 oracle 的比较；三个变异（改成按整数比较、andnot 不屏蔽、不清零未用字）都会被 -G 以 5 抓出；-Q 输出逐字节不变。
一、3e4b4c1 字段组集合 GW 2 字，MAXG 124，在写任何字段位集之前检查；70 组的 capg 由负例转为正例（-d 3,267 B、UNS2 280 B 与 Python 相同，140 键往返正确）；新负例 capgn（125 组）以 4 拒绝；__common 增加 953,344 B。
二、3a4c371 类别集合 CW 2 字，MAXC 96；63 类的 capc 转为正例（-d 483 B、UNS2 61 B）；新负例 capcn（97 类）以 4 拒绝。合成正例 capcf：8×9 键、96 个类、类名顺序与下标顺序完全相反、标签 (37(9a+b)+5) mod 96，72 条规则，18 个 factored 候选全部保留，选中 17 个单元的那个，所有桶的类别集合与名次集合都跨越第 61/62 位；三种构建都与 Python 相同（-d 3,986 B、UNS2 254 B）。三个变异（按下标排序、把名次集合当大整数比较、不排序）都会使 -d 与 Python 不一致，证明桶比较确实进入输出（一次性测量，记在 README）。__common 增到 34,745,400 B（主要是按 96 类分配的 cw 与 W2）；abi 峰值 RSS 19,988,480 B。
三、3f953aa irsel：原始键 420、商化键 414 分开记；-d 7,310 B、UNS2 919 B、发布节 903 B 都相同；420 键往返无并列；不变量负例触发；cc、unisacc、UBSan 都通过。分支：dlist，rep_factored 18 次全部返回 None。
**未覆盖**：irsel 本身没有 T4，也没有保留的分解候选，所以类别比较只由 capcf 覆盖；capcf 也没走到补丁路径和提前拒绝。
**isel、combo 前置审计**（只读，未改代码）：MAXR 不是唯一阻挡。isel 只需 MAXR 93（symbol 头 93 条规则，92 实测以 4 退出）；ncand 峰值 74、total_units 峰值 97、H 73，都在上限内；Python build_net 0.67 s。combo 需要三项：MAXR 93；**MAXU 197**（pick 试探中 total_units 峰值 197，196 实测以 4 退出——此前矩阵漏列，与 abi 的 MAXCAND 同属中间量阻挡）；**MAXCAND 420**（T4 每轮为 symbol 与 sysno 重新追加候选，4 次选择合计 116/217/318/419，419 实测以 4 退出）。共享池 pall 峰值 122，离 128 只剩 6。其他各项（lv 最大 5、maxlogit 19、类别 94、组 69、头 15、键数）都不阻挡。草稿验证：93/197/420 时两个阶段的 -d 都与 Python 相同（isel 21,661 B，combo 80,910 B），UBSan 下也相同；96/200/432 时 UNS2 相同（1,129 B、3,224 B），往返无并列；原 16 个阶段不变。**静态内存**：现在 34,745,400 B；93/197/420 为 73,251,112 B；96/200/432 为 76,331,448 B，增长主要来自 cw[MAXCAND·MAXU][MAXC]；每个候选实际不超过 nr 行，把候选行宽从 MAXU 改为 MAXR，或压平成单元池，可以大幅减小。**批次时间**：isel 单独约 6 s，可并入第 2 批；combo 单独一批超过 60 s（alarm 60 实测被杀，分项估算约 85 s，其中 Python 参考 41.7 s：netdump -d 27.8 s、uns2slice 13.9 s），需要缓存 Python 参考或把 combo 拆成两批。**建议步骤**：一、MAXR 80 → 96 并更新规则门禁，验收 isel；二、先压缩候选行（行宽改 MAXR 或压平），现有容量下 17 个阶段不变；三、MAXU 128 → 200；四、MAXCAND 288 → 432；五、先解决 combo 的批次时间，再验收 combo。
**isel、combo 已达**（五步，各一个提交，独立复跑分批模式通过）——18 个阶段全部逐阶段通过：
一、MAXR 96：81 条规则的 capr81 由负例转为正例（-d 5,684 B、UNS2 371 B 与 Python 相同）；新负例 caprn（10×10 表，100 条规则）在规则检查处以 4 退出。isel：-d 21,661 B、UNS2 1,129 B、发布节 1,113 B，146 键 × 2 头无并列，三种构建都通过。
二、候选行压缩：候选行宽另名 MAXCU = MAXR，ccube、cw 的 21 处 ci·MAXU 下标、T4 的 hc·MAXU 下标、newunit 与 rep_factored 入口检查都改用它；newunit 显式防护 u ≥ nr 或 nr > MAXCU 即中止；README 论证 dlist 合并不增加规则数、k 前缀 ≤ nr、基础与补丁路径都在 cap = nr 返回。压缩前冻结的 408 个文件（17 个阶段加 20 张合成表，三种构建的 -d、-u、-t）改后 0 个不同。__common 34,974,776 → 27,438,136 B。
三、MAXU 200：共享池检查改为 4；新正例 capu（9^3 键、2 个头、162 个单元，旧上限下实测以 4 退出）与 Python 相同；新负例 capun（3 个头、243 个单元）在 total_units 处以 4 退出。共享池检查任何表都走不到（池就是选中组的单元，已被 total_units 限住），只由标明的测试入口 -T pool 覆盖，不算真实表反例。
四、MAXCAND 432：原 289 槽位负例 capk4 如实归类为正例（用 292 个槽位，-d 12,784 B 与 Python 相同）；新负例 capkn（6 个头）在第 433 个槽位以 4 退出。__common 39,664,056 B；combo 峰值 RSS 32,292,864 B（cc 构建，0.79 s）。
五、combo：-d 80,910 B、UNS2 3,224 B、发布节 3,208 B，438 键 × 15 头无并列，H = 99，三种构建都通过。检查按类别拆成三批（转储 45.1 s、UNS2 22.3 s、追踪与不变量 19.9 s）；子批只发分项回执，父进程按唯一的必需清单 need() 汇总 14 张分项回执后才发出完整的 combo 回执。故障测试（临时副本）：丢一类回执而 rc 为 0、多发一张完整回执、从计划删掉一批、子批一张回执都不打印、子批与父批请求同时丢一类——全部被拒。
**分批总览**：七批 42.7/10.2/30.6/15.3/45.1/22.3/19.9 s，回执并集 41 张 = 18 个阶段 + 23 项全局检查。第一批（含全局检查）已到 42.7 s，其中 capu 的 Python 参考约占 21 s，以后再加全局检查就要拆开。
**未覆盖**：newunit 的行防护与网络 H ≥ MAXU 的中止（已论证走不到）；共享池检查只由 -T pool 覆盖；rep_factored 入口 nr 超出 1..MAXCU 的检查走不到。
**边界**：18/18 只表示逐阶段通过；MAXS=8 的整包写出与跨阶段状态清理另立最后的验收，本轮未动。往返走 Python 的 UNS2 解码器，只在 macOS arm64 上测过。
**整包写出（已结案，见下方“整包闭合”；以下为当时的阻塞记录，保留作历史）**：MAXS 8 → 24，超限在任何构建之前以 4 拒绝（18 个表加 7 份 prec，真实负例）。一个进程从 18 份 TSV 写出的整包 9,124 B，在 cc、unisacc、UBSan 下都与 Python 参考、以及含 16 B 头部的 built.uns2 逐字节相同（version、nStages 18、nUnits 513、段顺序、名称集合、长度都逐项核对；排序在 C 里按名称做）。三种顺序（正常、逆序、固定乱序，54 次构建）输出相同；新增 resetstage() 在每个阶段开头显式清零各全局量，README 列出每个数组“读前必写”的依据和故意保留的进程级标志。整包往返：18 个阶段、8,484 个键、20,184 个（键, 头）对全部正确。写出失败新增退出码 7，全部是真实故障：输出路径是目录、目录不存在、ulimit -f 1 下的短写、以及在 fclose 时失败；重复段以 2、超 MAXS 以 4、空组以 2 退出。**阻塞**：unisacc 构建检测不到短写与关闭失败——自带 libc 的 include/stdio.h 中，fwrite 不带缓冲、忽略 __write 的返回值、总是返回 n；fclose 就是 close，fflush 是空操作。最小复现在 ulimit -f 1 下：cc 得到 3000、0、fclose −1，unisacc 与 Python 参考 VM 都得到 3000、5000、fclose 0；参考实现与二进制一致，所以不是误编译，而是库的语义缺口。wfail 这一批因此是红的，门禁没有合并。**待定**：是否修正 include/stdio.h（fwrite 返回实际写入字节数 / sz，并处理部分写与负返回），以及在无缓冲实现下“关闭失败”该在哪一步报告。
**整包闭合**（四个提交，合入主干至 HEAD，独立复跑通过）：变基到 fwrite 库修复 34f0859 之上后，11 批全部在 alarm 60 下通过（42.8/10.1/30.1/15.2/44.9/22.4/20.1/38.8/28.5/22.9/28.1 s），回执并集 48 张 = 18 个阶段 + 23 项全局检查 + 7 项整包检查（Python 参考、发布包、往返、cc/unisacc/UBSan 三种顺序、写失败），各一次。写失败按实际失败点验收：RLIMIT 整包（rlimpack）三种构建都在 fwrite 处失败；RLIMIT 单段 combo（rlimone）在 cc 与 UBSan 下于 fclose 失败（宿主 stdio 有缓冲），在 unisacc 下于 fwrite 失败（无缓冲），都以 7 退出并给出真实 IO 诊断——不把只触发写失败的用例计为关闭失败的覆盖。关闭失败的返回检查由测试入口 -T closefail 单独覆盖，标为 SIMULATED，走与生产相同的关闭路径，三种构建都以 7 退出。其余负例：输出路径是目录、目录不存在（7），重复段（2），超过 MAXS 24（4），空 -u 组（2），三种构建都通过。**意义**：18 张表 → C 构造 → UNS2 整包这一段闭合，整包与 built.uns2（含 16 B 头部）逐字节相同。**仍不是**：C MODEL 内核的部署、ckernel 全部产物的迁移、改表之后无 Python 的自我迭代闭环（J10 第 3、4 步）；跨平台未验证；预处理器差异单列未修。
**J10 第 3 步产物审计**（只读，未改代码）：kernel.sh 检查的 23 个文件 = emit-kernel 的 5 个 + gold-export 的 18 个 TSV。emit-kernel 读 weights/built.json（缺失时悄悄按 gold.py 规则重建并写回），从不读 built.uns2；在空的 scratch 目录重新生成的 5 个文件与 kernel/ 逐字节相同。**五个产物**：unisa_model.inc（225,605 B；S_* 阶段号按 gold.ALL；MODEL 由字段掩码与 7 字节条目组成，已验证由 built.uns2 构建与由 built.json 构建相同；DENSE 20,184 项来自对全域跑 predict，已验证等于 TSV 标签；model_dims；词表 TOKV..IRRECV 来自 gold.py 元组与 lex.py 的 TYPEKW；11 个阶段的 BF/BH 字符串写死在一个元组里；ENC_* 来自 catalog.ENCSPEC；头部是 gold.py、catalog.py、lex.py、built.json、ckernel.py 的 sha256 前 12 位，无时间戳）；unisa_cases.inc（686,786 B，已验证只由 TSV 重建即可逐字节相同）；unisa_core.c 与 unisa_self.c（都是 KERNEL_BODY 字符串模板）；unisa_headers.inc（19 个 include/*.h 几乎原样拷贝，与 gold、UNS2、TSV 无关）。**顺序**：编译器阶段号按 gold.ALL，UNS2 按名称排序，二者不同。**缺的元数据**：UNS2 缺阶段顺序、字段名、取值与类别字符串、头名；TSV 缺 ALL 顺序、TYPEKW、ENCSPEC、“词表名 → TSV 行”的映射、BF/BH 的 11 个阶段清单与 HEADS_MAX。**隐藏依赖**：_built() 从缓存回退到规则重建；_provenance 对缺失输入写“(absent)”而不失败；include/ 路径依赖当前目录；--check-oracle 只证明 DENSE 等于在 MODEL 上跑的 infer()，若 C 生成器也用同一个 infer() 算 DENSE，这项检查就什么也证明不了，DENSE 还必须对照 TSV 标签；C 工具若读取既有的 model.inc 就能抄答案，所以输入只能来自命令行，并在空目录运行。**建议首片**：只做 model.inc 里的 S_* 定义、MODEL、DENSE 与 model_dims；声明 iterate/kernel/order.tsv 作为阶段顺序的唯一权威（18 个阶段按 ALL 顺序加 HEADS_MAX，完整性对照 UNS2 名称集合与 gold.ALL）；genmodel.c 只打开命令行给出的 built.uns2、order.tsv、18 个 TSV 与输出路径，解码 UNS2、重排阶段、写 MODEL 与维度；DENSE 用真实的 infer() 在生成的 MODEL 上计算，并要求逐项等于 TSV 标签；Python 只作裁判，比较 scratch emit-kernel 输出的同一区域；cc 与 unisacc 双构建。判据：该区域逐字节相同；拼进 scratch 的 model.inc 后 --check-oracle 差异为 0；DENSE 20,184 项全部等于 TSV；在没有 kernel/ 与 built.json 的空目录里输出仍相同，删去任何一个输入都会失败。后续切片：词表与 BF/BH（TSV 加声明的名称映射）→ TYPEKW 与 ENCSPEC 作为声明的 TSV → cases.inc → headers.inc → KERNEL_BODY 改为 .c 文件 → 最后是来源头部。**待定**：来源头部要在 C 里复现 Python 源文件的 sha256，还是改头部格式。
**J10 第 3 步首片已达**（0197f45，独立复跑 iterate/kernel/check.sh 通过，11 张回执）：iterate/kernel/genmodel.c 只读命令行给出的 order.tsv、built.uns2 与 18 个 TSV（源码中只有两处 fopen：读参数、写输出），写出 model.inc 的 S_* 定义、MODEL、DENSE 与 model_dims()。order.tsv 是 C 生成路线的阶段号契约（18 个阶段加 heads_max 16），order_check.py 断言它等于 gold.ALL——过渡期 gold.ALL 仍是对照来源，不称唯一权威。DENSE 按声明顺序直接枚举 TSV 标签生成，不用推理结果。**区域比较**：从第一行 #define S_ 到 model_dims 的结束括号、去掉整行注释块，cc、unisacc、ASan+UBSan 三种构建都与同一提交下 scratch emit-kernel 的区域逐字节相同（204,496 B，2,514 行）。**语义**：把生成的区域拼进旧文件，得到“混合生成的临时 model.inc”（头部、词表、BF/BH、ENC 仍是旧的），与真实的 kernel/unisa_core.c 一起编译，cc 与 unisacc 下 18 个阶段、20,184 个（键, 头）判定全部正确、最大值唯一；在该混合文件上用 build_ref.sh 构建整个编译器，--check-oracle 20,184 问、0 差异。**空目录**：只放 built.uns2、gold/、order.tsv 时输出与在仓库根运行相同。**负例**：14 种坏输入（缺 TSV、缺 order.tsv、缺 UNS2、阶段重复、缺阶段、未知阶段、UNS2 截断两处、尾部多 1 字节、magic 错、取值数不符、类别数不符、TSV 给两次、TSV 阶段名被改）三种构建都以 1 拒绝且不写输出；3 种写失败以 7 退出（含 ulimit 下真实的短写 1,024/205,057 B）。**功能证据**：改一个 prec 标签而 UNS2 不变，语义检查拒绝（证明标签确实被读取；这不是第 4 步的自我迭代演示）；order.tsv 倒序后区域与同样倒序的 Python 裁判相同、S_COMBO=0…S_PP=17。**未做与缺口**：来源头部未实现（按已定的新格式留到后面，旧头部仍列出 genmodel 并不读取的 built.json）；内核与 sem.c 把 STAGE_NCLS 步长写死为 16，heads_max 不是 16 时 genmodel 能过而内核会错；词表、BF/BH、ENCSPEC、cases、headers、模板仍由 Python 生成。
**首片结案**（be7e41f 起）：heads_max 契约补齐——非 16 的值在解析时立即以 8 退出，早于容量检查、布局与打开输出；15、17 两个负例在三种构建下都被拒且不写输出；neg stride 把 KERNEL_HEADS 与 order.tsv 绑定到内核源码的 (s << 4) 下标。首片按“去整行注释后的区域逐字节相同、混合生成的临时 model.inc”口径结案，不扩大为整个文件脱离 Python。下一片（词表、BF/BH、HD）只做只读的声明映射设计，设计回报后再定实现。
**第二片设计（词表、BF/BH、HD；只读，未改代码）**：model.inc 第 2536–2823 行，288 行、17,922 B，全部 ASCII。16 个词表中 15 个与某个 TSV 的 #field 或 #head 行完全一致、顺序与长度相同（TOKV=parse/tok，PRODV=parse/y，ACTV=lex/y，DIRV=pp/dir，PPACTV=pp/y，NTV=parse/nt，TYSV=type/t1，TOPSV=type/op，TYOUTV=type/y，SCTXV=scope/ctx，SKINDV=scope/kind，SACTV=scope/y，IRFAMV=irsel/family，IRFLAV=irsel/flavor，IRRECV=irsel/y）；TYPEV 来自 lex.py 的 TYPEKW，没有对应的 TSV 行，保持明确例外。BF/BH 覆盖写死的 11 个阶段，共 49 个字符串，逐一按位置核对全部与 TSV 一致；BH 目前取自 built.json 的类别顺序，已验证等于 TSV 的 #head 顺序，改后 TSV 成为唯一来源（UNS2 不带类别名）。**声明映射**：iterate/kernel/vocab.tsv，逐行给出 vocab 符号 → 阶段/field 或 head/名称，typekw 行标为例外，bfbh 行按现有 11 个阶段的顺序；完整性门禁要求阶段存在、名称存在、符号唯一、行种已知、恰好 16 行词表与 11 行 bfbh。值在 TSV，映射与顺序在 vocab.tsv，阶段号在 order.tsv，TYPEKW 暂留 lex.py。**序列化**：Python 不做任何转义，词表用 \0、BF/BH 用 \000 分隔；有 76 个 BF/BH 值以数字开头，Python 只靠始终写三位八进制才没出错；词表目前没有以数字开头的非首值，只是碰巧。C 侧规则：字节原样写出，遇到含引号、反斜杠、0x20–0x7e 以外字节、“??”、空值，或以 0–7 开头的非首个词表值，就以 1 拒绝，而不是转义。**比较区域**：从 ^char \*TOKV = 到第一行 ^char \*ENC_ 之前，去掉整行注释块；本片中 TYPEV 的两行取自 Python 一侧并显式排除在比较之外。**门禁**：三种构建逐字节相同、空目录、13 类负例、改一个 TSV 词表值的功能测试、拼入两片的混合 model.inc 通过 --check-oracle 与相关套件。估计 genmodel.c 增加 170–220 行（需要按阶段保存字符串，因为现在 load_tsv 复用缓冲区）。
**J10 第 3 步第二片已达**（三个提交，独立复跑 iterate/kernel/check.sh --batches 通过：6 批 5.4/4.7/4.8/4.6/6.5/35.4 s，18 项检查各一张回执）：genmodel 读 vocab.tsv（15 条 vocab、1 条 typekw 占位、11 条 bfbh），生成 15 个 TSV 词表与 11 个阶段的 BF/BH/HD；输出顺序由 vocab.tsv 的行序驱动，提取器靠显式锚点定位、不假定 TOKV 在最前。TYPEV 只写一个占位段，C 不读 Python 或旧 kernel；只有 scratch 集成器按标记拼入旧的 TYPEV。**区域**：12,661 B、222 行、156 个符号（词表 15×2，BF 21×2，BH/HD 28×3），cc、unisacc、ASan+UBSan 三种构建都与 Python 裁判逐字节相同。**负例**：17 类、三种构建共 51 例，都以 1 拒绝且不写输出（含非法标识符、大小写折叠冲突、与已生成符号 DENSE 冲突、非首个词表值以 0 或 7 开头）；以 8、9 开头被接受。**池测试**：先加载的 enc 与最后加载的 reloc 各改一个不同的名字，两者都保留，只有 BF_ENC_1 与 BF_RELOC_0 变化。**改名测试**：opinfo 中 add64 改为 addq，按映射预测变化位置为 BF_OPINFO_0 与 BH_OPINFO_BCLS，实测完全一致，MODEL 与 DENSE 不变。**集成**：两片生成区域加旧头部（796 B）、TYPEV 段（212 B）与 ENC 尾部（1,994 B，15 个符号）拼成的混合 model.inc，去掉整行注释后与现有文件相同；build_ref.sh 构建，--check-oracle 0 差异，sem 的 cc 与 unisacc 都全对，closure 582/0，nativeboot 通过。**仍由 Python 生成或保留**：来源头部、TYPEV（TYPEKW）、ENC_*、cases.inc、headers.inc、模板。
**J10 第 3 步 TYPEKW 片已达**（2a8cd8e、fd6426d，独立复跑 iterate/kernel/check.sh --batches 通过：7 批 4.9/4.6/4.8/4.7/6.5/5.8/34.9 s，21 项检查各一张回执）：新增 iterate/kernel/typekw.tsv（18 个值，顺序同 lex.TYPEKW），由命令行显式传给 genmodel（genmodel -o OUT order.tsv vocab.tsv typekw.tsv built.uns2 TSV…），vocab.tsv 的 typekw 行决定 TYPEV 在输出中的位置；C 里不写死这 18 个词。typekw_check.py 只作过渡兼容裁判（Python 仍用 lex.TYPEKW，所以在 Python 读取 typekw.tsv 之前有两个来源）；vocab_check.py 与 typekw_check.py 改为解析不到预期结构（含空清单）即以 1 失败，四个用例验证。**区域**：12,834 B、224 行、158 个符号（16 个词表 × 2、BF 21 × 2、BH/HD 28 × 3），TYPEV 纳入比较，Python 裁判、现有文件与 cc、unisacc、ASan+UBSan 三种构建都逐字节相同；空目录（只有 built.uns2、gold/、order.tsv、typekw.tsv、vocab.tsv）输出相同，NTYPEV 为 18。**负例**：11 类 × 三种构建（缺文件、重复值、空值、引号、反斜杠、0x80、??、非首值以 0 或 7 开头、typekw 行缺失或重复）都以 1 拒绝且不写输出；以 8、9 开头被接受。**功能**：把 short 改为 shrt、交换 int 与 char，TYPEV 都只按声明变化，兼容裁判都报告 DIFFERENCE 并以 1 退出。**集成**：混合文件现在只有来源头部（796 B，无符号）与 ENC 尾部（1,994 B：ENC_X86_{ALU2,SETCC,SHIFTEXT}、ENC_ARM_{ALU3,INVCOND}，各带 N 宏）取自旧文件；sem 与 --check-oracle 都是 20,184 问 0 错，closure 582/0，nativeboot 通过。消费者 ABI 限制（名字 TYPEV 与 NTYPEV 固定、NUL 分隔、无空值、前端只测成员关系、每表索引 1024 项、只有标识符形的值才能被匹配）已写入 README；genmodel 没有要求取值必须是标识符形。
**ENCSPEC 设计**（只读，未改代码）：ENC 尾部（model.inc 第 2824 行到文件末，63 行、1,994 B、15 个符号）是 5 张有序的“名字 → 非负整数”表加固定模板（catalog.py:210–230 为数据，ckernel.py:290–306 为模板；值一律以十进制输出，_V 越界返回 0 - 1）。表：ENC_X86_ALU2（u8，5 项）、ENC_X86_SETCC（u8，6 项）、ENC_X86_SHIFTEXT（u3，3 项）、ENC_ARM_ALU3（u32，8 项，Rd/Rn/Rm 字段须为 0）、ENC_ARM_INVCOND（u4，6 项）。消费者（back_encode.c 与 emit_x86.py、emit_arm.py）全部按名字查找，同一架构内的键集合须互不相交（分派有先后）；实测重排两张表后，6 个目标的编译结果与自编译都逐字节相同，所以顺序只影响字节、不影响行为；把 invcond 的 slt64 由 0xA 改成 0xB，只改 1 行，编出的程序只在两个 arm64 目标上各变 2 字节（osx 另因签名哈希变化）。**声明文件**：iterate/kernel/encspec.tsv，table 行（符号、架构、族、位宽）与 op 行（符号、键、值），文件顺序即输出顺序；genmodel 内置 5 个符号及其位宽作为消费者 ABI 校验。拒绝：表重复、键重复、未知表、架构与符号不符、缺表、空表、同架构两表键重叠、非法或超宽数值（只接受十进制或 0x 十六进制、无符号、无前导零、位数上限、逐位溢出检查）、键越出输入域；缺项对 genmodel 合法，由过渡裁判报告。数值先解析为整数，再由 genmodel 自己格式化为十进制输出，不把字符串拼进 C。_V 返回 long，最大值 2^32−1 在当前全部 LP64 目标上安全（LLP64 不安全，记为 ABI 说明）。**过渡裁判**：encspec_check.py 用 ast 读 catalog.py，结构缺失、为空、字面量键重复、非整数或负值都以 1 失败。**比较区域**：Python 侧从第一行 /* ENC_ 到文件末，genmodel 侧用新的起始标记；去掉整行注释、保留空行，1,510 B、58 行；符号恰好 15 个（含 _V 函数）。**完成判据**：三种构建逐字节相同；改值与重排按上面的预测只改对应行，裁判报 DIFFERENCE；**执行测试**：编译生成的区域，在 −1、0、N−1、N 与每个下标上调用 _V，并走 enc_ix/vfind 的按名查找，与 catalog.ENCSPEC 的转储对照（cc 与 unisacc 都要做）；空目录；负例；集成时混合文件只剩旧头部，在 scratch 里构建真实编译器（不在仓库里运行 build_ref.sh，因为它会改写受跟踪的 unisacc.c），--check-oracle、closure、nativeboot。估计新增约 470–520 行。**待定**：是否拒绝空表；保留独立裁判还是让 Python 直接读 encspec.tsv。
（原计划）下一片：tyinfo，唯一在其余上限之内的多头阶段，用它验证 T4 共享与 pick。门禁仍是字节、全域语义和部署不变量三项。“无（未试）”的 6 个单头阶段只是不受上限阻挡，不等于已经对齐。位集扩容（商化键 > 62）另立一项。

写 UNS2 之前必须核对的不变量：C 验证器算的是 `hv * W2`，而部署的 IntNet 是“激活 > 0 就加一次 W2”。两者只有在每个隐单元的激活都是 0 或 1、且 `b1 = 1 - 约束字段数` 时才一致。要在全域上显式检查这两条，并在写 UNS2 前做一次编码、解码的往返，核对实际部署的算术。
容量：C 构造器用一个 long 表示键集合，一个阶段最多 62 个商化键。这是与多头移植无关的独立限制；要先列出超限的阶段，再决定位集的移植范围。
第 2 步的风险：construct.py 大量使用集合、字典和排序，C 子集里要换成定长数组和显式排序；任何枚举顺序或排序稳定性的差异都会改变权重字节。停止条件：先移植最小的阶段（prec、reloc），做到逐字节相同后再扩展；若某个阶段两次尝试仍无法对齐，就把它记为待办，不为全绿而修改 Python 侧的构造规则。

**K. 抽象、复用与归档（P1，2026-09-25，主人：“解决完速度，下一步梳理代码的抽象与复用”）**

每一步都以“对 HEAD 逐字节相同”为判据：C 侧 918 个镜像（examples、tests/c、tests/c99 与编译器自身，-O0/-O2，三个目标），Python 侧 closure 570/0、stages 95、optpy 192/0；自举 nativeboot、bigclosure 6/0。

| # | 内容 | 状态 |
|---|---|---|
| K1 | 删死代码：C 七个函数（29 行）、Python 十二个函数（132 行）、`unisa.sh`；`tests/gen2.sh` 归档（nativeboot 已取代） | 已达 |
| K2 | `src/` 由两个文件按段切成七个：`front_pp`、`front_parse`、`opt`、`main`、`back_lower`、`back_encode`、`back_image`；拼接顺序即依赖，`unisacc.c` 逐字节不变 | 已达 |
| K3 | 一个字符串相等（后端的 `bk_str_is` 是 `strsame` 的拷贝，107 处）；优化器六处手写的 out2 写字节合成 `ol_c`；peep 助手 `pp_` → `pk_`（`pp_` 本是预处理器的前缀） | 已达 |
| K4 | walker 的 `eimm`/`eframe`（25 处）；整行一次交给 `es()`，因为 `_rev` 配方按同一串里的逗号找操作数 | 已达 |
| K5 | cli、run、multi、selfgap、kernel 改用 `tests/lib.sh`；顺带修正 cli/run 在 snap 下用的是 `/tmp/ua_ref` 而非快照的 `$UA` | 已达 |
| K6 | `unisa/bits.py`：四份 `_round`、五处 64 位掩码合一 | 已达 |
| K7 | SGD 对照臂的 `weights/*.unisa` 移入 archive | **不做**：`--drive`、训练输出与 acceptance 都按路径读它们，约 150 KB，收益小于改动面 |
| K8 | 复查后不合并：`ud_same`（到行终止符）、`pk_same`（两段等长）、`vsame`（NUL 结尾词表）契约不同；帧槽 load/store 约 20 处的符号写在文本里（`[r6-`/`[r7+`），合成助手要多一个符号参数且须保住 `-0`，得不偿失。其余套件迁 `lib.sh` 随改随迁 | 结案 |


**明确不做**：目标文件与链接器（多单元已由一个 walker 解决）；训练（对照臂）；C11/C23 中 G 组之外的特性。

**进度（2026-09-25 01:30，编译速度）**：B3 的速度一半已达。`make com` **182 s → 17 s**：Python 后端两处 O(n·m)（`size()` 每条指令重建 5k 标签字典、`lower()` 每个 pc 扫全部标签），镜像逐字节不变。出货二进制编译自己 **2.5 s → 1.0 s**、cc -O2 构建 **264 → 90 ms**：采样定位 93% 在 `vfind`/`srcfind`/`sfind` 三个线性查找（`V_NLISTS` 16 小于实际的 18 个词表，多出的永远走线性），改为哈希，六目标镜像、全部探针与 220 个 c-testsuite 程序对旧编译器逐字节相同，gen2 = gen1。验证：closure 564/0、stages/selfhost 94、native 94/0、nativeboot、bigclosure 6/0、ape 3/0。**套件慢与发热的主因不是编译器**：每个新写出的二进制首次执行被 XProtect 扫描 0.5–0.9 s（`XprotectService` ~35% CPU），编译一个探针只要 0.01 s；十三个套件逐探针写-签-执行新文件。对策（`-run` 取代落盘执行、或主人把终端加入“开发者工具”）待定，见 AGENTS.md。
**进度（2026-09-25 02:30，60 秒上限）**：单次运行不超过 60 s。`corpus` 加 `SHARD=k/n`，all.sh 与 `make corpus` 分四片跑，每片约 20 s，合计 216 pass / 4 knownfail 与基线一致（一片不能改基线，但会报它那一份里丢掉的程序）。`fuzz` 改为 par.sh 并行，cc 的参考输出按“源码 + cc 版本”的哈希缓存：热启动 13 s，160/160；冷启动一次性超过 60 s。c99 55、warn 4/0、tools 11/11 重跑为绿。
**进度（2026-09-25 02:00，两个出货 bug）**：把 closure 的宿主执行改成 `-run`（省掉一次 XProtect 扫描）时抓到 `b_libc` 在仓库外死循环——出货的 `unisacc.com` 同样。① 按需补头文件（autoinc）只试 `./include/`，仓库外什么都没补上；改为像 `#include` 一样退回到内嵌副本。② C 前端对**没有任何单元定义的函数调用**不报错，后端把缺失标签解析为偏移 0，程序跳回自己入口；Python 前端一直拒绝。现在所有单元走完后报 `unisacc: error: undefined function 'X'`（链接器形制，调用单元的文本已不在）。`cli` 加两项（仓库外无 #include 调 libc；未定义函数被拒），旧编译器上两项都失败。369 个探针/c-testsuite 程序退出码与镜像不变；multi、diag、cli 24/0、hostile、closure 564/0、stages、selfhost、nativeboot、bigclosure 全绿。规则：单次运行上限 60 s（AGENTS.md）。
**进度（2026-09-25 00:31，机器过热中断处）**：A1 A2 C1 C2 E3 B1 A3 D1 D2 C3 C4 已达；未做 B3（重测体积/速度）、B2（.com Windows 自解压）、E1 E2（论文）、F1（make release 自动起停 VM）、G1–G3（_Generic / 语句表达式 / 初始化器）、D2 剩余的 fseek/ftell/remove/rename/getenv（需 catalog 加三个 syscall 行）。
**C4 提交时的验证状态**（最后一次完整 `all.sh` 被中断两次，机器过热）：C4 之后逐个跑过且绿的——c99 55/55、fuzz 160/160、tools 11/11、corpus 216/220（4 knownfail）、diag 14/14、hostile 21/21、warn 4/4 + 语料假阳性 0、layout 4572/0、datashape 12/0；**未在最终树上重跑**的——closure/stages/selfhost/ccrun/native（探针套件）、bigclosure、nativeboot、acceptance、selfgap 汇总行。C4 顺带改了五处类型事实（`*p` 宽度、`?:`、`&&`、枚举常量、`[1]` 成员），这些正是探针套件与 bigclosure 会判的东西——**下次开工第一件事：`./tests/all.sh`（约 14 分钟，跑完才输出）**，绿了再动 B3。

**顺序**：A1（便宜、直接回应论文局限）→ C1（便宜、摩擦最大）→ B1（杠杆最大、风险最大，尽早做以便充分回归）→ A3 → D → C3/C4 → B2 → E → G。F2 等主人决定，不阻塞其余。

**S-14 发布门。** 发布曾是一串靠人记住的动作：跑套件、留意 crossnative 是不是悄悄跳过了目标、
构建 `.com`、试跑、推送、打标签。可检查的部分现在由 `tests/release.sh` 检查：树已提交（脏树里
构建出来的东西无法从它自称的标签重建）、二进制报得出自己的版本、`STRICT=1` 下全部套件绿、
**没有任何套件被跳过**。它不推送、不打标签、不上传——那些是人来决定的部分。

**S-10 0.0.6 计划**（2026-09-23 定，承 0.0.5）。按"离判据多远 ÷ 成本"排，每项都带一条可测的完成判据——
没有判据的条目不进这张表。

| # | 事项 | 为什么 | 完成判据 |
|---|---|---|---|
| 1 | **体积：x86 rel8 短跳转** —— **已实现，判据未达，已如实改写**（2026-09-24）。两个后端同一套松弛轮次，逐字节一致（closure 540/540、bigclosure 6/6）；编译自身不变慢（227 ms）。但只省了编译器自身 x86 text 的 **1.2%**，不是计划写的 ≥5%——**估算错了**：unisacc.c 里可松弛的跳转只有 5,219 条。直方图显示 text 的真正大头是 tape 栈机的 push/pop：`.frame` 占 20.8%，紧随的 `load64/store64` 再占 16%——每次 push 是 `sub r10,8; mov [r10],rax` 共 7 字节。对应的杠杆写进 0.0.7（见下） | rel8 ✅；≥5% 改由 0.0.7 的 regmap 方案承担 |
| 2 | **体积：切片间共享** —— **已测量，判据按实测改写**（2026-09-24）。四个 Unix 切片：gzip 各自（现状）504 KB；bzip2 各自 417 KB（−17%）；**xz 放一个流 220 KB（−56%，这才是真正的共享）**——但 **macOS 不自带 xz**，bzip2 在精简 Linux 上也不保证有，换压缩器等于让一部分机器跑不了 `.com`，不做。更根本的是：`.com` 约 62% 是**未压缩的 PE**（Windows 原地执行，不能压），所以只动 Unix 切片整体最多降 6–7%，≥15% 这条从一开始就到不了。真正的办法写进 0.0.7 | 不做可移植性倒退 ✅；≥15% 改由 0.0.7 的自解压存根承担 |
| 3 | **扩表：GNU 语句表达式与 `_Generic`** —— **重新划界**（2026-09-24）。做到的：corpus 214 → **216/220**。00200 修的是 **gold 本身**：有符号整数运算的结果类型规则是 [G-2] 的旧捷径（`int + int` 是 long），按 C99 6.3.1.8 改正后重建权重、枚举重验全 1.000；00206 暴露的是**两个前端都不按位置处理宏定义**（C89 行为，`#undef` 在 C 前端根本没实现），已修。挪到 0.0.7 的三项、及各自真正需要什么：**00219 `_Generic`** 要类型系统区分**指针目标的限定符**（`const int *` 不可匹配 `int *`）与 `char`/`signed char`/`unsigned char`——横跨两个 walker 的类型改造；**00213/00214 语句表达式**要两个 walker 都能把块当表达式求值（含块内标签与 goto）；**00216** 是 tcc 的初始化器酷刑测试（花括号省略、多余花括号、混合指定初始化，282 行）。三项都在 C99 之外，各是一块独立的大活，挤进 0.0.6 会把体积两项挤掉 | corpus 216/220 ✅；余下 4 个写进 0.0.7 |
| 4 | **Python 前端的诊断追平 C 前端** | `CError` 仍然报它自己缓冲区的行号，而 C 前端已经能给出用户文件的行列与源码行（[S-12]）。两个前端的报错质量不对等 | `diag.sh` 的五个用例对 Python 前端也成立 |
| 5 | **`prd.tree.md` / `prd.map.md` 的生成器** —— **已达**（2026-09-24）：`python3 -m unisa docs`，三份文档的阶段表为生成区，[A-48] 检查 | 两份文件全靠手抄，11→14 那次重构之后它们落后了一整天而没有任何机制发现。阶段表在四个文件里各存一份 | 一条命令从 `unisa/gold.py` 生成这两份文件里的表；套件检查它们与 `unisa acc` 一致（像 [A-43] 检查常量那样） |
| 6 | **后端常量的检查面扩大** —— **已达**（2026-09-24）：[A-43] 从 17 个数扩到 30 个，覆盖三种镜像写出器的基址、页、头长与导入数；C 侧用十进制写（`5368709120` 即 `pe.IMAGEBASE`），正是肉眼最看不出漂移的地方 | [A-43] 现在核 17 个数。镜像头部、段对齐、PE 的 `SizeOfImage` 这类两边各写一遍的数字还没进去 | `consts_check.py` 覆盖三种镜像写出器里所有两侧重复的常量 |
| 7 | **`-S`（发射汇编）与 `-c` 的语义对齐** —— **已决定并落地**（2026-09-24）：`-S` 写 tape（tape 就是本编译器汇编层的 IR，与 gcc 的 `-S` 同级）；`-c` 保留为同义词，因为套件一直这么写，但 usage 行明说"没有目标文件"。不做目标文件：多单元已由一个 walker 解决（[A-30]） | 现在 `-c` 是"写 tape"，而 gcc/tcc 的 `-c` 是"写目标文件"。这个不一致会绊到把 unisacc 塞进 Makefile 的人 | 要么改名（`-c` → `--tape`）并让 `-c` 报一条明确的"没有链接器"的错，要么实现目标文件；**先写清楚选哪条** |
| 8 | **第二意见：GitHub CI 跑起来** —— **已达**（2026-09-24）：`fff9d7d` 起 Linux + macOS-14 + macOS-15 三个 job 全绿。它**确实是第二意见**：本机全绿时 CI 抓到了四个本机看不见的问题——oracle 缓存比对哈希而非问题（clang/gcc 对有符号溢出 UB 的不同处理让错答案挪位置）、复合字面量初始化的指针只存了 4 字节（栈上碰巧对）、glibc 导出真实的 `__mmap` 让参照程序崩溃、旧版 clang 拒绝 `case` 后直接跟声明。仍然**不产出任何交付物** | ✅ |
| 9 | **C99 补到 47/47** —— **已达**（2026-09-24）：`c99.knownfail` 已空 | 这是**初心那一条**："能编译并运行 C99 程序"。`corpus 214/220` 太宽容（那批程序又短又重叠）；[A-44] 按标准自己的变更清单建了 47 个特性探针，今天 **35/47 = 74%**。12 个缺口全部可定位、可枚举 | `c99.sh` 47/47，且 `difftest`、`corpus`、`closure` 不退 |
| 10 | **编译速度第二轮** —— **已达**（2026-09-23）：自编译 467 → 218 ms | 第一轮（2026-09-23）把 catalog 与词表改成一次性建索引，编译自己从 2.85 s 降到 **0.46 s**（6.2×，输出逐字节不变）。剖析里新的头部是 `lex`（168 样本）与 `infer`（45） | 编译自己 ≤0.25 s（cc -O2 构建），`bench` 棘轮不退；**输出必须逐字节不变** |
| 11 | **出货二进制的代码质量** | 同一份源码：cc -O2 编出的 unisacc 编译自己 0.46 s，**unisacc 自己编出的要 17.57 s** —— 慢 38×。这不是算法问题，是我们发的机器码没有窥孔与寄存器分配 | 先**量**，不急着做：`bench` 把这个比值记成一个数。要不要做优化器是一次范围决定，见下 |

**第 9 项的 12 个缺口**（`tests/c99/` 实测，2026-09-23）：

| 缺口 | 报错 | 性质 |
|---|---|---|
| `_Bool` / `<stdbool.h>` 的 `bool` | unknown identifier | **真语言缺口**，真实代码常用 |
| 逗号运算符 `(a++, b++, c)` | expected ')' | **真语言缺口** |
| `__func__` | unknown identifier | C99 要求的预定义标识符 |
| 柔性数组成员 `char d[]` | a constant is required here | C99 新增 |
| `##__VA_ARGS__`（空可变参数） | expected ')' | 实为 GNU 扩展，但普及到近乎必需 |
| `_Pragma` 运算符 | expected ')' | 冷门 |
| 十六进制浮点字面量 `0x1.8p1` | expected ';' | 冷门 |
| `<inttypes.h>` / `PRId64` | 头文件没带 | **只是少一个头文件** |
| `va_copy` | 缺 | C99 新增 |
| `strtod` 家族 | 缺 | 库 |
| 通用字符名标识符 `caf\u00e9` | 输出不符 | 冷门 |
| 宽字符相关 | 部分 | 冷门 |

**第 11 项是一次范围决定，不是技术债。** 做优化器意味着引入一整层"结构性代码"，而它对论点没有增量价值——论点是"表状决策可被构造的网络替代"，窥孔优化不是表状决策。建议：**只量不做**，把 38× 这个比值写进 `bench` 当作公开的已知代价。

不在 0.0.6 里、且要说清楚为什么：
- **训练**不在任何一条上——它是对照臂（[U-5]），碰它只会发热。
- **目标文件格式与链接器**不做：多翻译单元已经用"一个 walker 走多个单元"解决了（[A-30]），做 ELF 可重定位目标文件是另一个项目。
- **提交历史里的邮箱**（99 个提交带本机主机名）要不要重写，是仓库主人的决定，不是技术债。

**S-9 仪表要能证伪自己。** 一个套件如果只比对答案，它证明不了答案是**被使用**的
（[A-36] 的由来，见 E-57）；一个仪表如果自己崩溃却把崩溃算成通过，它比没有还糟——所以
`ablate.sh` 开跑前先自证，`all.sh` 用每个套件的**退出码**而不是最后一行文本定胜负。

**S-8 每一项都要落成棘轮。** 像 `corpus.baseline`、`selfgap.baseline` 那样只能升不能降，于是"完成度"不再是一个拍脑袋的百分比，而是套件里的数字。**绿色不等于覆盖**——一个套件只能证明它真的问过的问题，见 E-43。

---

---

# 第五部分 · 知识沉淀

