# Invalid input this compiler does not diagnose yet, one per line:
# <case> <reason>.  A case that STARTS being diagnosed fails the suite, so
# the list cannot rot; closing one means deleting a line.
#
# Empty today.  Two entries were here when the suite was written -- an
# unterminated comment and a struct containing itself by value -- and both
# were closed the same afternoon, which is the point of writing them down
# rather than remembering them.
