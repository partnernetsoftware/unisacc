# Programs in c-testsuite that this compiler is not trying to pass, one per
# line: <id> <reason>.  They are counted separately; a knownfail that starts
# PASSING is a failure, so the list cannot rot.
00123  floating point: this subset has no float or double
00174  floating point: this subset has no float or double
00175  floating point: this subset has no float or double
00195  floating point: this subset has no float or double
00204  floating point: homogeneous float aggregates in the arm64 ABI
00206  #pragma push_macro/pop_macro is a GCC/MSVC extension, not C99
00213  statement expressions `({ ... })` are a GCC extension, not C99
00214  statement expressions `({ ... })` are a GCC extension, not C99
00216  `struct {}` with no members is a GCC extension; C99 requires one
00219  _Generic is C11, not C99
00200  [G-2]: int arithmetic is evaluated at 64 bits, so sizeof(i+0) is 8
