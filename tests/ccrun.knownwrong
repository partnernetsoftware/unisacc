# Probes that unisacc COMPILES but answers differently from the Python front
# end, one per line: <name> <reason>.  They are counted separately; a
# knownwrong that starts AGREEING is a failure, so the list cannot rot.
#
# Being on this list is worse than being refused: the C compiler produces a
# program, and the program is wrong.  That is the whole reason ccrun runs the
# answers rather than counting what compiles.
b_args8       more arguments than argument registers: the C side has no stacked calling convention
b_frame       %ld of a 64-bit local: the C side's printf prints the low word
b_libc        <string.h> needs the on-demand include the Python driver does
b_printf      field width and flags: the C side's printf has no padding
b_shift       shift result type: the C side promotes neither operand [G-2 corrected]
b_unsigned2   unsigned narrow arithmetic: the C side has no unsigned tracking
