# Paper A：来自编译器实现侧的修改建议

2026-09-25 前记在这里的 8 条建议已全部并入 [`unisacc-paper.md`](unisacc-paper.md)：

- 摘要改为不随表增长而变的表述；
- 新增 §3.6 确定性；
- §2.2 补上“结构提出，表来裁决”；
- §8.1 补上代价表的裁判；
- §3.2 补上表的数据形态；
- §3.5 补上推理的编译形态；
- §8 补上覆盖度与容量。

论文 A 由本仓库的 unisacc 会话直接维护，这个文件只作新建议的暂存处。

## 2026-09-25：产品收敛与投稿证据

目标已校准为：种子产出编译器，编译器能够自举，并可靠编译支持范围内的 C99 项目。原生自举已不依赖 Python；APE 打包工具使用 Python。`iterate/` 的构造与生成工具属于开发工作，不是产品自举的前提；不以继续迁移这些工具作为论文 A 投稿条件。

正文已区分历史实验与当前发布候选。最终投稿前只补齐以下有限记录：

- 冻结源码提交、`unisacc.com` 的 SHA-256 与大小、实际构建命令。
- 同一候选的本地验收日志：自举、差分、真实项目与产品入口；每个平台分别记录通过、失败或未运行，跳过不计通过。
- 对照论文中的数值、平台和产品边界；旧性能记录若无法恢复输入与版本，保留为历史材料，不作为当前版本的定量结论。

2026-09-26 更新：v0.0.7 已发布，正文新增独立版本证据表；已核对发布页提交 `f301df3` 与 GitHub 附件的大小、SHA-256。平台覆盖引用发布说明，未在本次文档工作中重新跑测试。原始日志归档与历史性能版本对应仍须在投稿前核对，不能仅凭发布摘要认定论文全部证据已齐。本文的网络与表等价性不扩张为完整 C99 正确性，自举不扩张为自主改进能力；δ 草稿不并入论文。
