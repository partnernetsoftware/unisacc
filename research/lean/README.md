# UnisaFormal (Paper A)

Lean 4 contract kernel. See [`../formalization-roadmap.md`](../formalization-roadmap.md).

```bash
lake build
```

Toolchain: `lean-toolchain` pins `v4.14.0` (same as host elan default used to bootstrap).
